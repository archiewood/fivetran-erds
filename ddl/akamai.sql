CREATE TABLE entity_optins (
    id text,
    entity_id text,
    entity_uuid text,
    optin_creation_date text,
    optin_id text,
    optin_status text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, entity_id, entity_uuid),
    FOREIGN KEY (entity_id) REFERENCES entity(id),
    FOREIGN KEY (entity_uuid) REFERENCES entity(uuid)
);

CREATE TABLE entity_post_login (
    id text,
    entity_id text,
    entity_uuid text,
    privacy_policy_accepted_date text,
    privacy_policy_id text,
    screen_name text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, entity_id, entity_uuid),
    FOREIGN KEY (entity_id) REFERENCES entity(id),
    FOREIGN KEY (entity_uuid) REFERENCES entity(uuid)
);

CREATE TABLE entity_client (
    id text,
    entity_id text,
    entity_uuid text,
    client_id text,
    first_login text,
    last_login text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, entity_id, entity_uuid),
    FOREIGN KEY (entity_id) REFERENCES entity(id),
    FOREIGN KEY (entity_uuid) REFERENCES entity(uuid)
);

CREATE TABLE entity (
    id text,
    uuid text,
    about_me text,
    birthday text,
    children text,
    company text,
    consumer_id text,
    created text,
    current_location text,
    deactivate_account text,
    display text,
    display_name text,
    drupal_roles text,
    email text,
    email_verified text,
    family_name text,
    gender text,
    given_name text,
    graduation_year text,
    last_login text,
    last_updated text,
    licensing_state text,
    match_type text,
    middle_name text,
    optin_user text,
    originating_brand text,
    password_created text,
    password_type text,
    practice_name text,
    primary_address_address_1 text,
    primary_address_address_2 text,
    primary_address_address_type text,
    primary_address_city text,
    primary_address_company text,
    primary_address_country text,
    primary_address_mobile text,
    primary_address_phone text,
    primary_address_phone_type text,
    primary_address_state_abbreviation text,
    primary_address_unit text,
    primary_address_zip text,
    primary_address_zip_plus_4 text,
    privacy_policy_id text,
    product_1 text,
    profession text,
    professional_status text,
    promo_1 text,
    promo_code text,
    promotion_source text,
    reason_code text,
    sample_ability text,
    source_id text,
    specialty text,
    state_license_number text,
    tracking_campaign text,
    tracking_medium text,
    tracking_source text,
    type_name text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, uuid)
);
