CREATE TABLE country (
    id text,
    code text,
    continent text,
    is_eu text,
    "name" text,
    PRIMARY KEY (id)
);

CREATE TABLE pricelist_price (
    id text,
    pricelist_id text,
    product_id text,
    product_variant_id text,
    price_conversion_date text,
    price_conversion_rate text,
    price_formatted_value text,
    price_value text,
    price_value_in_cents text,
    recommended_retail_price_conversion_date text,
    recommended_retail_price_conversion_rate text,
    recommended_retail_price_formatted_value text,
    recommended_retail_price_value text,
    recommended_retail_price_value_in_cents text,
    PRIMARY KEY (id, pricelist_id),
    FOREIGN KEY (pricelist_id) REFERENCES pricelist(id),
    FOREIGN KEY (product_id) REFERENCES product(id),
    FOREIGN KEY (product_variant_id) REFERENCES product_variant(id)
);

CREATE TABLE product_size_stock (
    _fivetran_id text,
    product_size_id text,
    warehouse_id text,
    allocated_quantity text,
    available_now_quantity text,
    available_quantity text,
    demand_quantity text,
    free_to_allocate_quantity text,
    incoming_quantity text,
    linked_incoming_quantity text,
    on_delivery_quantity text,
    physical_quantity text,
    unlinked_incoming_quantity text,
    unshipped_quantity text,
    PRIMARY KEY (_fivetran_id),
    FOREIGN KEY (product_size_id) REFERENCES product_size(id),
    FOREIGN KEY (warehouse_id) REFERENCES warehouse(id)
);

CREATE TABLE voucher (
    id text,
    store_id text,
    code text,
    combine_with_other_discount text,
    combine_with_other_voucher text,
    conversion_html text,
    created_at text,
    entry_point_strategy text,
    max_usages text,
    method text,
    "name" text,
    priority text,
    reuse text,
    start_at text,
    "status" text,
    stop_at text,
    successful_usages text,
    "type" text,
    updated_at text,
    url text,
    PRIMARY KEY (id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE lowest_price_market (
    lowest_price_fivetran_id text,
    market_id text,
    PRIMARY KEY (lowest_price_fivetran_id, market_id),
    FOREIGN KEY (lowest_price_fivetran_id) REFERENCES lowest_price(_fivetran_id),
    FOREIGN KEY (market_id) REFERENCES market(id)
);

CREATE TABLE lowest_price (
    _fivetran_id text,
    pricelist_id text,
    product_variant_id text,
    original_price_conversion_date text,
    original_price_conversion_rate text,
    original_price_formatted_value text,
    original_price_value text,
    original_price_value_in_cents text,
    price_conversion_date text,
    price_conversion_rate text,
    price_formatted_value text,
    price_value text,
    price_value_in_cents text,
    start_date text,
    PRIMARY KEY (_fivetran_id),
    FOREIGN KEY (pricelist_id) REFERENCES pricelist(id),
    FOREIGN KEY (product_variant_id) REFERENCES product_variant(id)
);

CREATE TABLE newsletter_subscription (
    id text,
    product_size_id text,
    product_variant_id text,
    store_id text,
    created_at text,
    email text,
    is_active text,
    PRIMARY KEY (id),
    FOREIGN KEY (product_size_id) REFERENCES product_size(id),
    FOREIGN KEY (product_variant_id) REFERENCES product_variant(id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE voucher_usage (
    _fivetran_id text,
    order_id text,
    voucher_id text,
    code text,
    "date" text,
    params_json text,
    url text,
    voucher_method text,
    voucher_name text,
    PRIMARY KEY (_fivetran_id),
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (voucher_id) REFERENCES voucher(id)
);

CREATE TABLE brand (
    id text,
    external_id text,
    "name" text,
    uri text,
    PRIMARY KEY (id)
);

CREATE TABLE mapped_attribute (
    id text,
    description text,
    "name" text,
    object_type text,
    PRIMARY KEY (id)
);

CREATE TABLE invoice_payment (
    id text,
    invoice_id text,
    created_at text,
    payment_date text,
    "type" text,
    value_conversion_date text,
    value_conversion_rate text,
    value_formatted_value text,
    value_value text,
    value_value_in_cents text,
    PRIMARY KEY (id),
    FOREIGN KEY (invoice_id) REFERENCES invoice(id)
);

CREATE TABLE product_attribute_element (
    "index" text,
    product_attribute_index text,
    product_id text,
    description text,
    is_multi text,
    "key" text,
    kind text,
    selected_value text,
    selected_value_name text,
    "value" text,
    PRIMARY KEY ("index", product_attribute_index, product_id),
    FOREIGN KEY (product_attribute_index) REFERENCES product_attribute("index"),
    FOREIGN KEY (product_id) REFERENCES product_attribute(product_id)
);

CREATE TABLE product_attribute (
    "index" text,
    product_id text,
    description text,
    object_type text,
    PRIMARY KEY ("index", product_id),
    FOREIGN KEY (product_id) REFERENCES product(id)
);

CREATE TABLE product (
    id text,
    brand_id text,
    created_at text,
    external_id text,
    harmonized_commodity_code text,
    harmonized_commodity_code_description text,
    internal_comment text,
    is_bundle text,
    is_serializable_product text,
    "name" text,
    number text,
    "status" text,
    updated_at text,
    weight text,
    weight_formatted_value text,
    weight_unit text,
    weight_value text,
    PRIMARY KEY (id),
    FOREIGN KEY (brand_id) REFERENCES brand(id)
);

CREATE TABLE product_variant_display (
    id text,
    product_variant_id text,
    "comment" text,
    description text,
    meta_description text,
    meta_keywords text,
    meta_title text,
    minimum_order_quantity text,
    "name" text,
    order_quantity_denominator text,
    short_description text,
    "status" text,
    store_item_id text,
    tags jsonb,
    updated_at text,
    uri text,
    PRIMARY KEY (id, product_variant_id),
    FOREIGN KEY (product_variant_id) REFERENCES product_variant(id)
);

CREATE TABLE product_variant_media (
    id text,
    product_variant_id text,
    metadata jsonb,
    source_mime_type text,
    source_type text,
    source_url text,
    PRIMARY KEY (id, product_variant_id),
    FOREIGN KEY (product_variant_id) REFERENCES product_variant(id)
);

CREATE TABLE product_variant (
    id text,
    product_id text,
    external_id text,
    internal_name text,
    "name" text,
    "status" text,
    stock_offset text,
    unit_cost_conversion_date text,
    unit_cost_conversion_rate text,
    unit_cost_formatted_value text,
    unit_cost_value text,
    unit_cost_value_in_cents text,
    updated_at text,
    variant_number text,
    PRIMARY KEY (id),
    FOREIGN KEY (product_id) REFERENCES product(id)
);

CREATE TABLE store_brand (
    brand_id text,
    store_id text,
    PRIMARY KEY (brand_id, store_id),
    FOREIGN KEY (brand_id) REFERENCES brand(id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE product_size (
    id text,
    product_variant_id text,
    "comment" text,
    description text,
    ean text,
    is_active text,
    preorder text,
    quantity text,
    size_number text,
    sku text,
    upc text,
    updated_at text,
    PRIMARY KEY (id),
    FOREIGN KEY (product_variant_id) REFERENCES product_variant(id)
);

CREATE TABLE store (
    id text,
    allocation_rule_id text,
    allocation_rule_is_active text,
    allocation_rule_name text,
    allocation_rule_type text,
    "name" text,
    "type" text,
    PRIMARY KEY (id)
);

CREATE TABLE warehouse (
    id text,
    brick_and_mortar_id text,
    country_id text,
    state_id text,
    address text,
    address_2 text,
    allocation_policy text,
    city text,
    external_id text,
    is_consignation text,
    is_hidden_from_stock_view text,
    "name" text,
    "status" text,
    stock_master text,
    stock_ownership text,
    threshold text,
    zip_code text,
    PRIMARY KEY (id),
    FOREIGN KEY (brick_and_mortar_id) REFERENCES brick_and_mortar(id),
    FOREIGN KEY (country_id) REFERENCES country(id),
    FOREIGN KEY (state_id) REFERENCES country_state(id)
);

CREATE TABLE market_campaign (
    campaign_id text,
    market_id text,
    PRIMARY KEY (campaign_id, market_id),
    FOREIGN KEY (campaign_id) REFERENCES campaign(id),
    FOREIGN KEY (market_id) REFERENCES market(id)
);

CREATE TABLE country_state (
    id text,
    country_id text,
    code text,
    "name" text,
    PRIMARY KEY (id, country_id),
    FOREIGN KEY (country_id) REFERENCES country(id)
);

CREATE TABLE market_assigned_to_country (
    country_id text,
    market_id text,
    PRIMARY KEY (country_id, market_id),
    FOREIGN KEY (country_id) REFERENCES country(id),
    FOREIGN KEY (market_id) REFERENCES market(id)
);

CREATE TABLE market (
    id text,
    store_id text,
    allocation_rule_id text,
    allocation_rule_is_active text,
    allocation_rule_name text,
    allocation_rule_type text,
    "comment" text,
    external_id text,
    "name" text,
    updated_at text,
    PRIMARY KEY (id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE pricelist (
    id text,
    "comment" text,
    is_default_for_currency text,
    "name" text,
    "status" text,
    updated_at text,
    PRIMARY KEY (id)
);

CREATE TABLE brick_and_mortar_store (
    brick_and_mortar_id text,
    store_id text,
    PRIMARY KEY (brick_and_mortar_id, store_id),
    FOREIGN KEY (brick_and_mortar_id) REFERENCES brick_and_mortar(id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE customer_order (
    customer_id text,
    order_id text,
    PRIMARY KEY (customer_id, order_id),
    FOREIGN KEY (customer_id) REFERENCES customer(id),
    FOREIGN KEY (order_id) REFERENCES orders(id)
);

CREATE TABLE campaign_variant (
    id text,
    campaign_id text,
    created_at text,
    discount_percent text,
    show_news text,
    show_sale text,
    start_at text,
    "status" text,
    stop_at text,
    "type" text,
    PRIMARY KEY (id, campaign_id),
    FOREIGN KEY (campaign_id) REFERENCES campaign(id)
);

CREATE TABLE campaign (
    id text,
    created_at text,
    "name" text,
    "status" text,
    updated_at text,
    PRIMARY KEY (id)
);

CREATE TABLE order_line (
    id text,
    order_id text,
    line_campaign_id text,
    product_size_id text,
    cancelled_quantity text,
    "comment" text,
    discount_percent text,
    external_id text,
    folder_name text,
    frontend_item_id text,
    has_any_discount text,
    line_campaign_discount_conversion_date text,
    line_campaign_discount_conversion_rate text,
    line_campaign_discount_formatted_value text,
    line_campaign_discount_percent text,
    line_campaign_discount_value text,
    line_campaign_discount_value_in_cents text,
    line_campaign_fixed_price_conversion_date text,
    line_campaign_fixed_price_conversion_rate text,
    line_campaign_fixed_price_formatted_value text,
    line_campaign_fixed_price_value text,
    line_campaign_fixed_price_value_in_cents text,
    line_campaign_name text,
    line_campaign_original_discount_conversion_date text,
    line_campaign_original_discount_conversion_rate text,
    line_campaign_original_discount_formatted_value text,
    line_campaign_original_discount_value text,
    line_campaign_original_discount_value_in_cents text,
    line_campaign_show_news text,
    line_campaign_show_sale text,
    line_campaign_start_at text,
    line_campaign_stop_at text,
    line_campaign_type text,
    line_value_conversion_date text,
    line_value_conversion_rate text,
    line_value_formatted_value text,
    line_value_value text,
    line_value_value_in_cents text,
    product_brand text,
    product_external_url text,
    product_name text,
    product_number text,
    product_variant_name text,
    quantity text,
    returned_quantity text,
    "size" text,
    tax_percent text,
    unit_original_price_conversion_date text,
    unit_original_price_conversion_rate text,
    unit_original_price_formatted_value text,
    unit_original_price_value text,
    unit_original_price_value_in_cents text,
    unit_price_conversion_date text,
    unit_price_conversion_rate text,
    unit_price_formatted_value text,
    unit_price_value text,
    unit_price_value_in_cents text,
    PRIMARY KEY (id, order_id),
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (line_campaign_id) REFERENCES campaign(id),
    FOREIGN KEY (product_size_id) REFERENCES product_size(id)
);

CREATE TABLE orders_attribute_element (
    "index" text,
    order_id text,
    orders_attribute_index text,
    description text,
    is_multi text,
    "key" text,
    kind text,
    selected_value text,
    selected_value_name text,
    "value" text,
    PRIMARY KEY ("index", order_id, orders_attribute_index),
    FOREIGN KEY (order_id) REFERENCES orders_attribute(order_id),
    FOREIGN KEY (orders_attribute_index) REFERENCES orders_attribute("index")
);

CREATE TABLE orders_attribute (
    "index" text,
    order_id text,
    description text,
    object_type text,
    PRIMARY KEY ("index", order_id),
    FOREIGN KEY (order_id) REFERENCES orders(id)
);

CREATE TABLE orders (
    id text,
    billing_address_country_id text,
    billing_address_state_id text,
    shipping_address_country_id text,
    shipping_address_state_id text,
    store_id text,
    allocated_warehouse_count text,
    billing_address_address_1 text,
    billing_address_address_2 text,
    billing_address_attention text,
    billing_address_cell_phone_number text,
    billing_address_city text,
    billing_address_company_name text,
    billing_address_ec_vat_number text,
    billing_address_email text,
    billing_address_fax_number text,
    billing_address_firstname text,
    billing_address_lastname text,
    billing_address_phone_number text,
    billing_address_state_or_province text,
    billing_address_vat_number text,
    billing_address_zip_code text,
    created_at text,
    currency_base_rate text,
    external_id text,
    has_pending_allocation_requests text,
    internal_comment text,
    is_internal text,
    is_locked text,
    is_on_hold text,
    is_waiting_for_payment text,
    number text,
    order_date text,
    other_comment text,
    payment_method_external_id text,
    payment_method_id text,
    payment_method_name text,
    payment_method_plugin text,
    payment_method_status text,
    payment_method_type text,
    payment_method_uri text,
    shipping_address_address_1 text,
    shipping_address_address_2 text,
    shipping_address_attention text,
    shipping_address_cell_phone_number text,
    shipping_address_city text,
    shipping_address_company_name text,
    shipping_address_ec_vat_number text,
    shipping_address_email text,
    shipping_address_fax_number text,
    shipping_address_firstname text,
    shipping_address_lastname text,
    shipping_address_phone_number text,
    shipping_address_state_or_province text,
    shipping_address_vat_number text,
    shipping_address_zip_code text,
    "status" text,
    tax_breakdown_description text,
    tax_breakdown_tax_included text,
    tax_breakdown_tax_percent text,
    tax_conversion_date text,
    tax_conversion_rate text,
    tax_formatted_value text,
    tax_value text,
    tax_value_in_cents text,
    total_cancelled_quantity text,
    total_discount_conversion_date text,
    total_discount_conversion_rate text,
    total_discount_formatted_value text,
    total_discount_value text,
    total_discount_value_in_cents text,
    total_handling_conversion_date text,
    total_handling_conversion_rate text,
    total_handling_formatted_value text,
    total_handling_value text,
    total_handling_value_in_cents text,
    total_line_conversion_date text,
    total_line_conversion_rate text,
    total_line_formatted_value text,
    total_line_value text,
    total_line_value_in_cents text,
    total_original_quantity text,
    total_quantity text,
    total_shipping_conversion_date text,
    total_shipping_conversion_rate text,
    total_shipping_formatted_value text,
    total_shipping_value text,
    total_shipping_value_in_cents text,
    total_tax_added_conversion_date text,
    total_tax_added_conversion_rate text,
    total_tax_added_formatted_value text,
    total_tax_added_value text,
    total_tax_added_value_in_cents text,
    total_tax_deducted_conversion_date text,
    total_tax_deducted_conversion_rate text,
    total_tax_deducted_formatted_value text,
    total_tax_deducted_value text,
    total_tax_deducted_value_in_cents text,
    total_tax_included_conversion_date text,
    total_tax_included_conversion_rate text,
    total_tax_included_formatted_value text,
    total_tax_included_value text,
    total_tax_included_value_in_cents text,
    total_unexpedited_quantity text,
    updated_at text,
    user_ip text,
    PRIMARY KEY (id),
    FOREIGN KEY (billing_address_country_id) REFERENCES country(id),
    FOREIGN KEY (billing_address_state_id) REFERENCES country_state(id),
    FOREIGN KEY (shipping_address_country_id) REFERENCES country(id),
    FOREIGN KEY (shipping_address_state_id) REFERENCES country_state(id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE invoice_line (
    id text,
    invoice_id text,
    order_line_id text,
    brand_name text,
    discount_percent text,
    harmonized_commodity_code text,
    harmonized_commodity_description text,
    product_name text,
    product_number text,
    product_variant_name text,
    quantity text,
    "size" text,
    tax_percent text,
    PRIMARY KEY (id, invoice_id),
    FOREIGN KEY (invoice_id) REFERENCES invoice(id),
    FOREIGN KEY (order_line_id) REFERENCES order_line(id)
);

CREATE TABLE invoice (
    id text,
    account_id text,
    state_id text,
    additional_information text,
    billing_address_1 text,
    billing_address_2 text,
    billing_address_attention text,
    billing_address_cell_phone_number text,
    billing_address_city text,
    billing_address_company_name text,
    billing_address_ec_vat_number text,
    billing_address_email text,
    billing_address_fax_number text,
    billing_address_firstname text,
    billing_address_lastname text,
    billing_address_phone_number text,
    billing_address_state_or_province text,
    billing_address_vat_number text,
    billing_address_zip_code text,
    commissionconversion_date text,
    commissionconversion_rate text,
    commissionformatted_value text,
    commissionvalue text,
    commissionvalue_in_cents text,
    company_registration_number text,
    created_at text,
    currency_base_rate text,
    discount_conversion_date text,
    discount_conversion_rate text,
    discount_formatted_value text,
    discount_value text,
    discount_value_in_cents text,
    due_date text,
    external_id text,
    is_overdue text,
    line_values_conversion_date text,
    line_values_conversion_rate text,
    line_values_formatted_value text,
    line_values_value text,
    line_values_value_in_cents text,
    number text,
    order_date text,
    our_reference text,
    pdf_url text,
    pre_paid_conversion_date text,
    pre_paid_conversion_rate text,
    pre_paid_formatted_value text,
    pre_paid_value text,
    pre_paid_value_in_cents text,
    purchase_order_number text,
    shipping_address_1 text,
    shipping_address_2 text,
    shipping_address_attention text,
    shipping_address_cell_phone_number text,
    shipping_address_city text,
    shipping_address_company_name text,
    shipping_address_ec_vat_number text,
    shipping_address_email text,
    shipping_address_fax_number text,
    shipping_address_firstname text,
    shipping_address_lastname text,
    shipping_address_phone_number text,
    shipping_address_state_or_province text,
    shipping_address_vat_number text,
    shipping_address_zip_code text,
    shipping_conversion_date text,
    shipping_conversion_rate text,
    shipping_formatted_value text,
    shipping_value text,
    shipping_value_in_cents text,
    "status" text,
    tax_added_conversion_date text,
    tax_added_conversion_rate text,
    tax_added_formatted_value text,
    tax_added_value text,
    tax_added_value_in_cents text,
    "type" text,
    updated_at text,
    PRIMARY KEY (id),
    FOREIGN KEY (account_id) REFERENCES account(id),
    FOREIGN KEY (state_id) REFERENCES country_state(id)
);

CREATE TABLE brick_and_mortar (
    id text,
    state_id text,
    address text,
    display_name text,
    email text,
    latitude text,
    longitude text,
    "name" text,
    phone_number text,
    "status" text,
    "type" text,
    website text,
    PRIMARY KEY (id),
    FOREIGN KEY (state_id) REFERENCES country_state(id)
);

CREATE TABLE affiliate (
    id text,
    store_id text,
    html_snippet text,
    "name" text,
    "status" text,
    updated_at text,
    uri text,
    PRIMARY KEY (id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE category (
    id text,
    store_id text,
    display_sort_type text,
    is_top_category text,
    meta_description text,
    meta_keywords text,
    meta_title text,
    "name" text,
    "status" text,
    uri text,
    PRIMARY KEY (id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE "return" (
    id text,
    order_id text,
    store_id text,
    "comment" text,
    created_at text,
    created_from text,
    currency_base_rate text,
    external_id text,
    grand_total_formatted_value text,
    grand_total_value text,
    grand_total_value_in_cent text,
    return_status text,
    returned_to_stock text,
    total_quantity text,
    PRIMARY KEY (id),
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);

CREATE TABLE account_attribute_element (
    "index" text,
    account_attribute_index text,
    account_id text,
    description text,
    is_multi text,
    "key" text,
    kind text,
    selected_value text,
    selected_value_name text,
    "value" text,
    PRIMARY KEY ("index", account_attribute_index, account_id),
    FOREIGN KEY (account_attribute_index) REFERENCES account_attribute("index"),
    FOREIGN KEY (account_id) REFERENCES account_attribute(account_id)
);

CREATE TABLE account_attribute (
    "index" text,
    account_id text,
    description text,
    object_type text,
    PRIMARY KEY ("index", account_id),
    FOREIGN KEY (account_id) REFERENCES account(id)
);

CREATE TABLE account (
    id text,
    account_address_state_id text,
    billing_address_state_id text,
    account_address_1 text,
    account_address_2 text,
    account_address_attention text,
    account_address_cell_phone_number text,
    account_address_city text,
    account_address_company_name text,
    account_address_ec_vat_number text,
    account_address_email text,
    account_address_fax_number text,
    account_address_first_name text,
    account_address_last_name text,
    account_address_phone_number text,
    account_address_state_or_province text,
    account_address_vat_number text,
    account_address_zip_code text,
    account_email text,
    allow_consignation text,
    apply_credit_limit text,
    attribute_description text,
    attribute_object_type text,
    billing_address_1 text,
    billing_address_2 text,
    billing_address_attention text,
    billing_address_cell_phone_number text,
    billing_address_city text,
    billing_address_company_name text,
    billing_address_ec_vat_number text,
    billing_address_email text,
    billing_address_fax_number text,
    billing_address_firstname text,
    billing_address_lastname text,
    billing_address_phone_number text,
    billing_address_state_or_province text,
    billing_address_vat_number text,
    billing_address_zip_code text,
    billing_email text,
    block_if_unpaid_invoices text,
    carrier_information_account_identifier text,
    carrier_information_carrier_name text,
    carrier_information_service_name text,
    carrier_information_tax_id text,
    created_at text,
    discount_percent text,
    external_id text,
    has_brands_restriction text,
    internal_comment text,
    is_discount_visible text,
    is_internal text,
    "name" text,
    other_comment text,
    shipping_email text,
    "status" text,
    updated_at text,
    vat_number text,
    website_url text,
    PRIMARY KEY (id),
    FOREIGN KEY (account_address_state_id) REFERENCES country_state(id),
    FOREIGN KEY (billing_address_state_id) REFERENCES country_state(id)
);

CREATE TABLE customer_attribute_element (
    "index" text,
    customer_attribute_index text,
    customer_id text,
    description text,
    is_multi text,
    "key" text,
    kind text,
    selected_value text,
    selected_value_name text,
    "value" text,
    PRIMARY KEY ("index", customer_attribute_index, customer_id),
    FOREIGN KEY (customer_attribute_index) REFERENCES customer_attribute("index"),
    FOREIGN KEY (customer_id) REFERENCES customer_attribute(customer_id)
);

CREATE TABLE customer_attribute (
    "index" text,
    customer_id text,
    description text,
    object_type text,
    PRIMARY KEY ("index", customer_id),
    FOREIGN KEY (customer_id) REFERENCES customer(id)
);

CREATE TABLE customer (
    id text,
    billing_state_id text,
    store_id text,
    billing_address_1 text,
    billing_address_2 text,
    billing_address_attention text,
    billing_address_cell_phone_number text,
    billing_address_city text,
    billing_address_company_name text,
    billing_address_ec_vat_number text,
    billing_address_email text,
    billing_address_fax_number text,
    billing_address_firstname text,
    billing_address_lastname text,
    billing_address_phone_number text,
    billing_address_state_or_province text,
    billing_address_vat_number text,
    billing_address_zip_code text,
    birthdate text,
    created_at text,
    email text,
    external_id text,
    first_name text,
    gender text,
    internal_comment text,
    is_anonymized text,
    is_registered text,
    last_login_at text,
    last_name text,
    last_online_at text,
    "status" text,
    total_orders text,
    updated_at text,
    website_url text,
    PRIMARY KEY (id),
    FOREIGN KEY (billing_state_id) REFERENCES country_state(id),
    FOREIGN KEY (store_id) REFERENCES store(id)
);
