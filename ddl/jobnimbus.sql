CREATE TABLE contact_owner (
    id text,
    contact_id text,
    PRIMARY KEY (id, contact_id),
    FOREIGN KEY (contact_id) REFERENCES contact(id)
);

CREATE TABLE contact (
    id text,
    actual_time text,
    address_line_1 text,
    address_line_2 text,
    approved_estimate_total text,
    approved_invoice_due text,
    approved_invoice_total text,
    attachment_count text,
    city text,
    class_id text,
    class_name text,
    color text,
    color_shade text,
    company text,
    country_name text,
    created_by text,
    created_by_name text,
    customer text,
    date_created text,
    date_end text,
    date_start text,
    date_status_change text,
    date_updated timestamp,
    description text,
    disable_auto_text text,
    display_name text,
    email text,
    estimated_time text,
    external_id text,
    fax_number text,
    field_assist jsonb,
    first_name text,
    home_phone text,
    image_id text,
    is_active text,
    is_archived text,
    is_closed text,
    is_lead text,
    is_sub_contractor text,
    is_user text,
    last_budget_gross_margin text,
    last_budget_gross_profit text,
    last_budget_revenue text,
    last_estimate text,
    last_estimate_date_created text,
    last_estimate_date_estimate text,
    last_estimate_jnid text,
    last_estimate_number text,
    last_invoice text,
    last_invoice_date_created text,
    last_invoice_date_invoice text,
    last_invoice_jnid text,
    last_invoice_number text,
    last_name text,
    latitude text,
    location_id text,
    location_name text,
    location_parent_id text,
    longitude text,
    merged text,
    mobile_phone text,
    number text,
    open_edge_id text,
    recid text,
    record_type text,
    record_type_name text,
    related jsonb,
    rules jsonb,
    sales_rep text,
    sales_rep_name text,
    "source" text,
    source_name text,
    state_text text,
    "status" text,
    status_name text,
    subcontractors jsonb,
    sunlight_event text,
    sunlight_status_and_id text,
    tags jsonb,
    task_count text,
    "type" text,
    website text,
    work_phone text,
    zip text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id)
);

CREATE TABLE task_owner (
    id text,
    task_id text,
    PRIMARY KEY (id, task_id),
    FOREIGN KEY (task_id) REFERENCES task(id)
);

CREATE TABLE task_related (
    id text,
    task_id text,
    "name" text,
    subject text,
    PRIMARY KEY (id, task_id),
    FOREIGN KEY (id) REFERENCES contact(id),
    FOREIGN KEY (task_id) REFERENCES task(id)
);

CREATE TABLE task (
    id text,
    actual_time text,
    all_day text,
    all_day_end_date text,
    all_day_start_date text,
    created_by text,
    created_by_name text,
    customer text,
    date_created text,
    date_end text,
    date_sort text,
    date_start text,
    date_updated timestamp,
    description text,
    estimated_time text,
    external_id text,
    hide_from_calendarview text,
    hide_from_tasklist text,
    is_active text,
    is_archived text,
    is_completed text,
    location_id text,
    merged text,
    number text,
    priority text,
    recid text,
    record_type text,
    record_type_name text,
    rules jsonb,
    subcontractors jsonb,
    tags jsonb,
    title text,
    "type" text,
    PRIMARY KEY (id)
);

CREATE TABLE file_owner (
    id text,
    file_id text,
    PRIMARY KEY (id, file_id),
    FOREIGN KEY (file_id) REFERENCES file(id)
);

CREATE TABLE file_related (
    file_id text,
    id text,
    "name" text,
    PRIMARY KEY (file_id, id),
    FOREIGN KEY (file_id) REFERENCES file(id),
    FOREIGN KEY (id) REFERENCES contact(id)
);

CREATE TABLE file (
    id text,
    content_type text,
    created_by text,
    created_by_name text,
    customer text,
    date_created text,
    date_file_created text,
    date_updated timestamp,
    description text,
    file_name text,
    is_active text,
    is_archived text,
    is_private text,
    record_type text,
    record_type_name text,
    sales_rep text,
    "size" text,
    sub_type text,
    "type" text,
    -- exif_* (dynamic column),
    PRIMARY KEY (id)
);

CREATE TABLE job_owner (
    id text,
    job_id text,
    PRIMARY KEY (id, job_id),
    FOREIGN KEY (job_id) REFERENCES job(id)
);

CREATE TABLE job_related (
    id text,
    job_id text,
    "name" text,
    subject text,
    PRIMARY KEY (id, job_id),
    FOREIGN KEY (id) REFERENCES contact(id),
    FOREIGN KEY (job_id) REFERENCES job(id)
);

CREATE TABLE job (
    id text,
    actual_time text,
    address_line_1 text,
    address_line_2 text,
    all_day text,
    all_day_end_date text,
    all_day_start_date text,
    approved_estimate_total text,
    approved_invoice_due text,
    approved_invoice_total text,
    attachment_count text,
    city text,
    class_id text,
    class_name text,
    country_name text,
    created_by text,
    created_by_name text,
    customer text,
    date_created text,
    date_end text,
    date_start text,
    date_status_change text,
    date_updated timestamp,
    description text,
    estimated_time text,
    external_id text,
    field_assist jsonb,
    image_id text,
    is_active text,
    is_archived text,
    is_closed text,
    is_lead text,
    last_budget_gross_margin text,
    last_budget_gross_profit text,
    last_budget_revenue text,
    last_estimate text,
    last_estimate_date_created text,
    last_estimate_date_estimate text,
    last_estimate_jnid text,
    last_estimate_number text,
    last_invoice text,
    last_invoice_date_created text,
    last_invoice_date_invoice text,
    last_invoice_jnid text,
    last_invoice_number text,
    latitude text,
    location_id text,
    location_parent_id text,
    longitude text,
    merged text,
    "name" text,
    number text,
    open_edge_id text,
    parent_approved_estimate_total text,
    parent_approved_invoice_due text,
    parent_approved_invoice_total text,
    parent_fax_number text,
    parent_home_phone text,
    parent_last_estimate text,
    parent_last_invoice text,
    parent_mobile_phone text,
    parent_work_phone text,
    recid text,
    record_type text,
    record_type_name text,
    rules jsonb,
    sales_rep text,
    sales_rep_name text,
    "source" text,
    source_name text,
    state_text text,
    "status" text,
    status_name text,
    subcontractors jsonb,
    sunlight_events text,
    sunlight_status_and_id text,
    tags jsonb,
    task_count text,
    "type" text,
    zip text,
    PRIMARY KEY (id)
);

CREATE TABLE activity_owner (
    id text,
    activity_id text,
    PRIMARY KEY (id, activity_id),
    FOREIGN KEY (activity_id) REFERENCES activity(id)
);

CREATE TABLE activity_related (
    activity_id text,
    id text,
    "name" text,
    subject text,
    PRIMARY KEY (activity_id, id),
    FOREIGN KEY (activity_id) REFERENCES activity(id),
    FOREIGN KEY (id) REFERENCES contact(id)
);

CREATE TABLE activity (
    id text,
    created_by text,
    created_by_name text,
    customer text,
    date_created text,
    date_updated text,
    email_status text,
    external_id text,
    is_active text,
    is_archived text,
    is_editable text,
    is_private text,
    is_status_change text,
    location_id text,
    location_parent_id text,
    merged text,
    note text,
    record_type text,
    record_type_name text,
    rules jsonb,
    sales_rep text,
    sales_rep_name text,
    "source" text,
    stack text,
    "type" text,
    PRIMARY KEY (id)
);
