CREATE TABLE click_and_commission_report (
    _fivetran_id text,
    currency text,
    "date" text,
    device_type text,
    ppc_clickout_count text,
    ppc_commission text,
    vertical text,
    PRIMARY KEY (_fivetran_id)
);
