CREATE TABLE time_collector (
    id text,
    created_by text,
    last_modified_by text,
    user_id text,
    booking_date timestamp,
    change_value text,
    collector_value text,
    created_date_time timestamp,
    end_date timestamp,
    last_modified_date_time timestamp,
    start_date timestamp,
    system_record_status text,
    time_collector_type text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE candidate_background_education (
    background_element_id text,
    candidate_id text,
    background_order_position text,
    degree text,
    degree_date timestamp,
    end_date timestamp,
    last_modified_date_time timestamp,
    major text,
    present_student text,
    school text,
    school_address text,
    school_city text,
    school_country text,
    school_phone text,
    school_state text,
    school_type text,
    school_zip text,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id)
);

CREATE TABLE background_memberships (
    background_element_id text,
    user_id text,
    background_order_position text,
    end_date timestamp,
    last_modified_date timestamp,
    organization text,
    "role" text,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE background_community (
    background_element_id text,
    user_id text,
    background_order_position text,
    end_date timestamp,
    last_modified_date timestamp,
    "name" text,
    "role" text,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE employee_compensation (
    start_date timestamp,
    user_id text,
    created_by text,
    last_modified_by text,
    benefits_rate double precision,
    bonus_target double precision,
    created_date_time timestamp,
    end_date timestamp,
    event text,
    event_reason text,
    is_eligible_for_benefits boolean,
    is_eligible_for_car boolean,
    is_highly_compensated_employee boolean,
    is_insider boolean,
    last_modified_date_time timestamp,
    pay_grade text,
    pay_group text,
    payroll_system_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (start_date, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE admin_reassign_offer_approver (
    application_id text,
    offer_detail_id text,
    user_id text,
    job_req_id text,
    target_user_id text,
    candidate_name text,
    job_req_title text,
    -- custom_* (dynamic column),
    PRIMARY KEY (application_id, offer_detail_id, user_id),
    FOREIGN KEY (application_id) REFERENCES job_application(id),
    FOREIGN KEY (offer_detail_id) REFERENCES job_offer(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (job_req_id) REFERENCES job_requisition(id),
    FOREIGN KEY (target_user_id) REFERENCES users(id)
);

CREATE TABLE candidate_tags (
    id text,
    candidate_id text,
    label text,
    locale text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id)
);

CREATE TABLE employee_pay_comp_non_recurring (
    pay_component_code text,
    pay_date timestamp,
    user_id text,
    created_by text,
    last_modified_by text,
    alternative_cost_center text,
    created_date_time timestamp,
    currency_code text,
    last_modified_date_time timestamp,
    notes text,
    operation text,
    "value" double precision,
    -- custom_* (dynamic column),
    PRIMARY KEY (pay_component_code, pay_date, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE per_person (
    person_id_external text,
    created_by text,
    last_modified_by text,
    user_id text,
    birth_name text,
    country_of_birth text,
    created_date_time timestamp,
    created_on timestamp,
    date_of_birth text,
    include_all_record text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    per_person_uuid text,
    person_id text,
    region_of_birth text,
    -- custom_* (dynamic column),
    PRIMARY KEY (person_id_external),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE emp_cost_distribution_item (
    emp_cost_distribution_effective_start_date timestamp,
    external_code text,
    emp_cost_distribution_users_sys_id text,
    created_by text,
    last_modified_by text,
    cost_center text,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    mdf_system_created_by text,
    mdf_system_created_date timestamp,
    mdf_system_effective_end_date timestamp,
    mdf_system_effective_start_date timestamp,
    mdf_system_entity_id text,
    mdf_system_last_modified_by text,
    mdf_system_last_modified_date timestamp,
    mdf_system_last_modified_date_with_tz text,
    mdf_system_object_type text,
    mdf_system_record_id text,
    mdf_system_record_status text,
    mdf_system_status text,
    mdf_system_transaction_sequence text,
    mdf_system_version_id text,
    percentage text,
    -- custom_* (dynamic column),
    PRIMARY KEY (emp_cost_distribution_effective_start_date, external_code, emp_cost_distribution_users_sys_id),
    FOREIGN KEY (emp_cost_distribution_users_sys_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE job_application_question_response (
    order_id text,
    application_id text,
    answer text,
    expected_answer text,
    expected_answer_value double precision,
    high_low text,
    max_length double precision,
    question text,
    question_response text,
    "type" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (order_id, application_id),
    FOREIGN KEY (application_id) REFERENCES job_application(id)
);

CREATE TABLE fo_job_function (
    external_code text,
    start_date timestamp,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    created_on timestamp,
    description text,
    description_de_de text,
    description_default_value text,
    description_en_debug text,
    description_en_gb text,
    description_en_us text,
    description_es_es text,
    description_fr_fr text,
    description_ja_jp text,
    description_ko_kr text,
    description_localized text,
    description_nl_nl text,
    description_pt_br text,
    description_pt_pt text,
    description_ru_ru text,
    description_zh_cn text,
    description_zh_tw text,
    end_date timestamp,
    job_function_type text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    mdf_system_record_id text,
    "name" text,
    name_de_de text,
    name_default_value text,
    name_en_debug text,
    name_en_gb text,
    name_en_us text,
    name_es_es text,
    name_fr_fr text,
    name_ja_jp text,
    name_ko_kr text,
    name_localized text,
    name_nl_nl text,
    name_pt_br text,
    name_pt_pt text,
    name_ru_ru text,
    name_zh_cn text,
    name_zh_tw text,
    parent_function_code text,
    "status" text,
    "type" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE job_requisition_screening_question (
    locale text,
    order_id text,
    job_req_id text,
    question_id text,
    question_parent_id text,
    disqualifier boolean,
    expected_answer_value double precision,
    expected_dir text,
    job_req_content text,
    max_length text,
    question_description text,
    question_parent_response text,
    question_weight double precision,
    rating_scale text,
    required boolean,
    score boolean,
    -- custom_* (dynamic column),
    PRIMARY KEY (locale, order_id, job_req_id),
    FOREIGN KEY (job_req_id) REFERENCES job_requisition(id),
    FOREIGN KEY (question_id) REFERENCES job_requisition_question(id),
    FOREIGN KEY (question_parent_id) REFERENCES job_requisition_question(id)
);

CREATE TABLE form_user_rating_comment (
    form_content_id text,
    form_data_id text,
    item_id text,
    rating_type text,
    section_index integer,
    user_id text,
    "comment" text,
    comment_key text,
    comment_label text,
    comment_permission text,
    first_name text,
    full_name text,
    last_name text,
    rating text,
    rating_key text,
    rating_label text,
    rating_permission text,
    text_rating text,
    -- custom_* (dynamic column),
    PRIMARY KEY (form_content_id, form_data_id, item_id, rating_type, section_index, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE background_inside_work_experience (
    background_element_id text,
    user_id text,
    background_order_position text,
    department text,
    end_date timestamp,
    last_modified_date timestamp,
    start_date timestamp,
    title text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE work_order (
    effective_start_date timestamp,
    user_id text,
    work_order_id text,
    created_by text,
    last_modified_by text,
    vendor text,
    billing_amount double precision,
    billing_rate text,
    created_date timestamp,
    created_date_time timestamp,
    currency text,
    effective_end_date timestamp,
    effective_status text,
    end_date timestamp,
    last_modified_date_time timestamp,
    start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_transaction_sequence text,
    system_version_id text,
    work_order_name text,
    work_order_owner_id text,
    worker_type text,
    -- custom_* (dynamic column),
    PRIMARY KEY (effective_start_date, user_id, work_order_id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (vendor) REFERENCES vendor_info(id)
);

CREATE TABLE background_check_result (
    id text,
    background_check_request_request_id text,
    created_date_time timestamp,
    final_step boolean,
    report_url text,
    step_message text,
    step_name text,
    step_status text,
    vendor_code text,
    vendor_order_no text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (background_check_request_request_id) REFERENCES background_check_request(id)
);

CREATE TABLE fo_company (
    external_code text,
    start_date timestamp,
    created_by text,
    last_modified_by text,
    country text,
    created_date_time timestamp,
    created_on timestamp,
    currency text,
    default_location text,
    default_pay_group text,
    description text,
    description_de_de text,
    description_default_value text,
    description_en_gb text,
    description_en_us text,
    description_es_es text,
    description_fr_fr text,
    description_ja_jp text,
    description_ko_kr text,
    description_localized text,
    description_nl_nl text,
    description_pt_br text,
    description_pt_pt text,
    description_ru_ru text,
    description_zh_cn text,
    description_zh_tw text,
    end_date timestamp,
    entity_oid text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    mdf_system_record_id text,
    "name" text,
    name_de_de text,
    name_default_value text,
    name_en_debug text,
    name_en_gb text,
    name_en_us text,
    name_es_es text,
    name_fr_fr text,
    name_ja_jp text,
    name_ko_kr text,
    name_localized text,
    name_nl_nl text,
    name_pt_br text,
    name_pt_pt text,
    name_ru_ru text,
    name_zh_cn text,
    name_zh_tw text,
    standard_hours double precision,
    "status" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE background_education (
    background_element_id text,
    user_id text,
    degree text,
    major text,
    school_country text,
    school_state text,
    background_order_position text,
    degree_date timestamp,
    end_date timestamp,
    last_modified_date timestamp,
    school text,
    school_address text,
    school_city text,
    school_type text,
    school_zip text,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (degree) REFERENCES pick_list_option(id),
    FOREIGN KEY (major) REFERENCES pick_list_option(id),
    FOREIGN KEY (school_country) REFERENCES pick_list_option(id),
    FOREIGN KEY (school_state) REFERENCES pick_list_option(id)
);

CREATE TABLE fo_department (
    external_code text,
    start_date timestamp,
    cost_center_nav_external_code text,
    created_by text,
    last_modified_by text,
    address_address_1 text,
    address_address_2 text,
    address_address_3 text,
    address_address_4 text,
    address_address_5 text,
    address_city text,
    address_country text,
    address_county text,
    address_province text,
    address_state text,
    address_zip_code text,
    company_flx text,
    cost_center text,
    created_date_time text,
    created_on timestamp,
    cust_to_legal_entity_prop text,
    description text,
    description_de_de text,
    description_default_value text,
    description_en_gb text,
    description_en_us text,
    description_es_es text,
    description_fr_fr text,
    description_ja_jp text,
    description_ko_kr text,
    description_localized text,
    description_nl_nl text,
    description_pt_br text,
    description_pt_pt text,
    description_ru_ru text,
    description_zh_cn text,
    description_zh_tw text,
    division_flx text,
    end_date timestamp,
    geozone_flx text,
    head_of_unit text,
    last_modified_date_time text,
    last_modified_on timestamp,
    location_group text,
    "name" text,
    name_de_de text,
    name_default_value text,
    name_en_gb text,
    name_en_us text,
    name_es_es text,
    name_fr_fr text,
    name_ja_jp text,
    name_ko_kr text,
    name_localized text,
    name_nl_nl text,
    name_pt_br text,
    name_pt_pt text,
    name_ru_ru text,
    name_zh_cn text,
    name_zh_tw text,
    parent text,
    standard_hours text,
    "status" text,
    timezone text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (cost_center_nav_external_code) REFERENCES fo_cost_center(external_code),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE candidate_background_inside_work_experience (
    background_element_id text,
    candidate_id text,
    background_order_position text,
    department text,
    end_date timestamp,
    last_modified_date_time timestamp,
    start_date timestamp,
    title text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id)
);

CREATE TABLE job_application_onboarding_status (
    id text,
    onboarding_id text,
    created_date timestamp,
    last_modified_date timestamp,
    message text,
    "name" text,
    "status" text,
    status_type text,
    "type" text,
    url text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (onboarding_id) REFERENCES job_application_onboarding(id)
);

CREATE TABLE background_var_pay_employee_hist_data (
    background_element_id text,
    basis double precision,
    business_goal_code text,
    country text,
    currency_code text,
    department text,
    division text,
    end_date timestamp,
    incentive_plan_code text,
    job_title text,
    last_modified_date timestamp,
    location text,
    pay_grade text,
    salary text,
    start_date timestamp,
    tgt_pct double precision,
    user_id text,
    var_pay_program_name text,
    PRIMARY KEY (background_element_id)
);

CREATE TABLE employment_termination (
    end_date timestamp,
    id text,
    user_id text,
    created_by text,
    last_modified_by text,
    benefits_end_date timestamp,
    bonus_pay_expiration_date timestamp,
    created_date_time timestamp,
    eligible_for_sal_continuation boolean,
    event_reason text,
    last_date_worked text,
    last_modified_date_time timestamp,
    new_main_employment_id text,
    notes text,
    ok_to_rehire boolean,
    payroll_end_date timestamp,
    regret_termination boolean,
    salary_end_date timestamp,
    stock_end_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (end_date, id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE employee_workflow_request (
    id text,
    wf_request_id text,
    action_type text,
    effective_date timestamp,
    entity_type text,
    event_reason text,
    request_type text,
    subject_id text,
    wf_config text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (wf_request_id) REFERENCES workflow_request(id)
);

CREATE TABLE job_requisition_posting (
    job_posting_id text,
    job_req_id text,
    last_modified_by text,
    posted_by text,
    agency_comments text,
    board_name text,
    channel_id text,
    external_partner_account_id text,
    last_modified_date_time timestamp,
    post_end_date timestamp,
    post_end_date_offset timestamp,
    post_start_date timestamp,
    post_start_date_offset timestamp,
    "status" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (job_posting_id, job_req_id),
    FOREIGN KEY (job_req_id) REFERENCES job_requisition(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (posted_by) REFERENCES users(id)
);

CREATE TABLE work_schedule (
    id text,
    created_by text,
    last_modified_by text,
    user_id text,
    average_hours_per_day text,
    average_hours_per_month text,
    average_hours_per_week text,
    average_hours_per_year text,
    average_working_days_per_week text,
    country text,
    created_date timestamp,
    created_date_time timestamp,
    external_name_default_value text,
    external_name_localized text,
    flexible_requesting_allowed boolean,
    individual_work_schedule boolean,
    last_modified_date_time timestamp,
    model_category text,
    period_model text,
    period_model_nav text,
    search_string text,
    shift_classification text,
    starting_date timestamp,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    time_recording_variant text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE background_award (
    background_element_id text,
    user_id text,
    background_order_position text,
    description text,
    institution text,
    issue_date timestamp,
    last_modified_date timestamp,
    "name" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE offer_letter (
    id text,
    application_id text,
    created_by text,
    last_modified_by text,
    body text,
    body_locale text,
    body_template_id text,
    bonus_payout_freq text,
    cand_response_comments text,
    cand_response_date timestamp,
    "comment" text,
    country_code text,
    country_name text,
    create_date timestamp,
    currency_code text,
    job_start_date timestamp,
    job_title text,
    last_modified_date timestamp,
    locale text,
    locale_code text,
    mailboxes text,
    offer_expiration_date timestamp,
    offer_letter text,
    offer_sent_date timestamp,
    overtime_rate text,
    salary_rate text,
    salary_rate_type text,
    send_mode text,
    "status" text,
    stock_grant text,
    stock_option text,
    subject text,
    target_bonus_amount double precision,
    target_bonus_percent text,
    template_id text,
    template_name text,
    tokens text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (application_id) REFERENCES job_application(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE onboarding_process (
    process_id text,
    created_by text,
    last_modified_by text,
    activities_config text,
    activities_status text,
    bpe_process_instance_id text,
    cancel_event_reason text,
    cancel_offboarding_reason text,
    cancel_onboarding_reason text,
    cancellation_comment text,
    cancellation_date timestamp,
    cancellation_source text,
    cancelled_due_to_restart boolean,
    created_date_time timestamp,
    employee_person_id text,
    end_date timestamp,
    last_modified_date_time timestamp,
    locale text,
    manager text,
    manager_person_id text,
    mdf_system_record_status text,
    offboarding_activities_config text,
    onb_2_master_id text,
    onboarding_auto_hire_status text,
    onboarding_hire_status text,
    onboarding_hired_date timestamp,
    onboarding_internal_hire boolean,
    process_restart_needed boolean,
    process_restarted boolean,
    process_status text,
    process_trigger text,
    process_type text,
    process_variant text,
    stable_id text,
    start_date timestamp,
    target_date timestamp,
    target_system text,
    "user" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (process_id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE workflow_request (
    id text,
    created_by text,
    last_modified_by text,
    parent_id text,
    created_date_time timestamp,
    current_step_num text,
    last_modified_date_time timestamp,
    module text,
    parent_wf_request_nav text,
    reminder_sent_date timestamp,
    "status" text,
    total_steps text,
    url text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (parent_id) REFERENCES workflow_request(id)
);

CREATE TABLE holiday (
    id text,
    country text,
    created_by text,
    last_modified_by text,
    created_date timestamp,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    name_default_value text,
    name_localized text,
    old_name text,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (country) REFERENCES country(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE external_time_record (
    id text,
    created_by text,
    last_modified_by text,
    user_id text,
    created_date timestamp,
    created_date_time timestamp,
    "date" timestamp,
    delete_external_time boolean,
    last_modified_date_time timestamp,
    "status" text,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE background_talent_pool (
    background_element_id text,
    user_id text,
    talent_poolitem text,
    background_order_position text,
    last_modified_date timestamp,
    start_date timestamp,
    talent_pool_comments text,
    talent_pool_item text,
    talent_pool_status text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (talent_poolitem) REFERENCES pick_list_option(id)
);

CREATE TABLE work_opportunity_tax_credit (
    id text,
    wotc_forms_url text,
    wotc_id text,
    wotc_screening_url text,
    wotc_status text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (id) REFERENCES job_application(id)
);

CREATE TABLE per_personal (
    person_id_external text,
    created_by text,
    last_modified_by text,
    attachment_id text,
    business_first_name text,
    business_last_name text,
    created_date_time timestamp,
    created_on timestamp,
    display_name text,
    end_date text,
    first_name text,
    gender text,
    include_all_records text,
    initials text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    last_name text,
    marital_status text,
    middle_name text,
    nationality text,
    native_preferred_lang text,
    operation text,
    person_nav_birth_name text,
    person_nav_country_of_birth text,
    person_nav_created_by text,
    person_nav_created_date_time text,
    person_nav_created_on text,
    person_nav_date_of_birth text,
    person_nav_include_all_records text,
    person_nav_last_modified_by text,
    person_nav_last_modified_date_time text,
    person_nav_last_modified_on text,
    person_nav_per_person_uuid text,
    person_nav_person_id text,
    person_nav_person_id_external text,
    person_nav_region_of_birth text,
    person_nav_user_id text,
    preferred_name text,
    salutation text,
    script text,
    since text,
    start_date text,
    title text,
    -- custom_* (dynamic column),
    PRIMARY KEY (person_id_external),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE currency_conversion (
    effective_start_date timestamp,
    id text,
    created_by text,
    last_modified_by text,
    to_currency text,
    base_currency text,
    conversion_rate text,
    created_date_time timestamp,
    effective_end_date timestamp,
    effective_status text,
    exchange_rate_type text,
    external_name_default_value text,
    external_name_localized text,
    last_modified_date_time timestamp,
    system_created_by text,
    system_created_date timestamp,
    system_entity_id text,
    system_last_modified_by text,
    system_last_modified_date timestamp,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (effective_start_date, id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (to_currency) REFERENCES currency(code)
);

CREATE TABLE employee_pay_comp_recurring (
    pay_component text,
    seq_number text,
    start_date timestamp,
    user_id text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    currency_code text,
    end_date timestamp,
    frequency text,
    last_modified_date_time timestamp,
    notes text,
    operation text,
    paycompvalue double precision,
    -- custom_* (dynamic column),
    PRIMARY KEY (pay_component, seq_number, start_date, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE currency (
    code text,
    effective_start_date timestamp,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    default_decimals text,
    description_default_value text,
    description_localized text,
    effective_end_date timestamp,
    external_name_default_value text,
    external_name_localized text,
    last_modified_date_time timestamp,
    "status" text,
    symbol text,
    system_created_by text,
    system_created_date timestamp,
    system_entity_id text,
    system_last_modified_by text,
    system_last_modified_date timestamp,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (code, effective_start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE job_application_status_label (
    id text,
    candidate_label text,
    locale text,
    status_label text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (id) REFERENCES job_application_status(id)
);

CREATE TABLE per_global_info_usa (
    country text,
    start_date timestamp,
    person_id_external text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    created_on timestamp,
    end_date timestamp,
    generic_date_1 timestamp,
    generic_date_10 timestamp,
    generic_date_2 timestamp,
    generic_date_3 timestamp,
    generic_date_4 timestamp,
    generic_date_5 timestamp,
    generic_date_6 timestamp,
    generic_date_7 timestamp,
    generic_number_1 text,
    generic_number_10 text,
    generic_number_11 text,
    generic_number_12 text,
    generic_number_13 text,
    generic_number_14 text,
    generic_number_15 text,
    generic_number_16 text,
    generic_number_2 text,
    generic_number_3 text,
    generic_number_4 text,
    generic_number_5 text,
    generic_number_6 text,
    generic_number_7 text,
    generic_number_8 text,
    generic_number_9 text,
    generic_string_1 text,
    generic_string_10 text,
    generic_string_11 text,
    generic_string_12 text,
    generic_string_13 text,
    generic_string_14 text,
    generic_string_15 text,
    generic_string_16 text,
    generic_string_2 text,
    generic_string_3 text,
    generic_string_4 text,
    generic_string_5 text,
    generic_string_6 text,
    generic_string_7 text,
    generic_string_8 text,
    generic_string_9 text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (country, start_date, person_id_external),
    FOREIGN KEY (person_id_external) REFERENCES per_person(person_id_external),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE candidate_background_talent_pool_corp (
    background_element_id text,
    background_order_position text,
    candidate_id text,
    talent_pool_item_corp text,
    talent_pool_status text,
    last_modified_date_time timestamp,
    start_date timestamp,
    talent_pool_comments text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, background_order_position, candidate_id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id),
    FOREIGN KEY (talent_pool_item_corp) REFERENCES pick_list_option(id),
    FOREIGN KEY (talent_pool_status) REFERENCES pick_list_option(id)
);

CREATE TABLE fo_translation (
    external_code text,
    created_by text,
    last_modified_by text,
    created_date timestamp,
    created_date_time timestamp,
    fo_field text,
    fo_objectid text,
    fo_type text,
    last_modified_date timestamp,
    last_modified_date_time timestamp,
    last_modified_date_with_tz timestamp,
    mdf_system_effective_end_date timestamp,
    mdf_system_effective_start_date timestamp,
    mdf_system_entity_id text,
    mdf_system_object_type text,
    mdf_system_record_id text,
    mdf_system_record_status text,
    mdf_system_status text,
    mdf_system_transaction_sequence text,
    mdf_system_version_id text,
    value_de_de text,
    value_default_value text,
    value_en_debug text,
    value_en_gb text,
    value_en_us text,
    value_es_es text,
    value_fr_fr text,
    value_ja_jp text,
    value_ko_kr text,
    value_localized text,
    value_nl_nl text,
    value_pt_br text,
    value_pt_pt text,
    value_ru_ru text,
    value_zh_cn text,
    value_zh_tw text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE external_personal_info (
    id text,
    first_name text,
    last_modified_date_time timestamp,
    last_name text,
    middle_name text,
    PRIMARY KEY (id)
);

CREATE TABLE job_requisition_operator (
    operator_role_id text,
    job_req_id text,
    user_id text,
    admin_selected_user_to_be_removed boolean,
    email text,
    fax text,
    first_name text,
    is_admin_selected boolean,
    last_name text,
    phone text,
    user_name text,
    -- custom_* (dynamic column),
    PRIMARY KEY (operator_role_id, job_req_id),
    FOREIGN KEY (job_req_id) REFERENCES job_requisition(id),
    FOREIGN KEY (user_id) REFERENCES pick_list_option(id)
);

CREATE TABLE background_promotability (
    background_element_id text,
    user_id text,
    background_order_position text,
    "comment" text,
    "function" text,
    last_modified_date timestamp,
    "level" text,
    timeframe text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE job_application_audit (
    field_order_position text,
    rev_number text,
    changed_by text,
    job_posting_id text,
    last_modified_by text,
    clob_field_ref text,
    clob_new_value text,
    clob_old_value text,
    date_new_value text,
    date_old_value text,
    field_id text,
    field_type text,
    job_post_end_date timestamp,
    job_post_start_date timestamp,
    last_modified_date timestamp,
    last_modified_external_id text,
    merged_from text,
    new_value text,
    old_value text,
    ref_type text,
    rev_type text,
    "source" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (field_order_position, rev_number),
    FOREIGN KEY (changed_by) REFERENCES users(id),
    FOREIGN KEY (job_posting_id) REFERENCES job_requisition_posting(job_posting_id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE form_template (
    id text,
    "name" text,
    "type" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id)
);

CREATE TABLE job_application_interview (
    id text,
    application_id text,
    cand_slot_map_id text,
    end_date timestamp,
    is_time_set integer,
    notes text,
    recruit_event_staff_id text,
    "source" text,
    start_date timestamp,
    "status" text,
    template_type text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (application_id) REFERENCES job_application(id)
);

CREATE TABLE background_certificates (
    background_element_id text,
    user_id text,
    license_country text,
    license_state text,
    background_order_position text,
    description text,
    end_date timestamp,
    institution text,
    last_modified_date timestamp,
    license_name text,
    license_number text,
    "name" text,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (license_country) REFERENCES pick_list_option(id),
    FOREIGN KEY (license_state) REFERENCES pick_list_option(id)
);

CREATE TABLE position_matrix_relationship (
    matrix_relationship_type text,
    position_effective_start_date timestamp,
    position_id text,
    created_by text,
    last_modified_by text,
    user_id text,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    related_position text,
    system_created_by text,
    system_created_date timestamp,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_last_modified_by text,
    system_last_modified_date timestamp,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (matrix_relationship_type, position_effective_start_date, position_id),
    FOREIGN KEY (position_id) REFERENCES "position"(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE job_application (
    id text,
    app_status_set_id text,
    candidate_id text,
    country_code text,
    job_code text,
    job_req_id text,
    last_modified_by text,
    referred_by text,
    user_id text,
    address text,
    address_2 text,
    agency_info text,
    app_locale text,
    app_status_set_item_id text,
    average_rating text,
    base_salary text,
    cand_conversion_processed text,
    cand_just text,
    cand_type_when_hired text,
    cell_phone text,
    city text,
    conf_number text,
    contact_email text,
    conviction_details text,
    country text,
    criminal_conviction_instructions text,
    criminal_conviction_label text,
    current_location text,
    current_title text,
    cust_ref_check_first_name text,
    cust_ref_check_last_name text,
    cust_ref_check_org text,
    cust_ref_check_phone text,
    cust_ref_check_title text,
    data_source text,
    date_avail text,
    date_of_birth timestamp,
    duplicate_profile text,
    exported_on text,
    external_title text,
    first_name text,
    former_employee boolean,
    gender text,
    have_you_been_convicted text,
    hire_date timestamp,
    hired_on text,
    home_phone text,
    induction_add text,
    induction_date timestamp,
    instr_additional_documents text,
    instr_app_additional_info text,
    instr_app_advice text,
    instr_app_return text,
    instr_cand_app_details text,
    instr_cand_approval text,
    instr_cand_assess text,
    instr_cand_fields text,
    instr_cand_pers_info text,
    instr_cand_screen text,
    instr_eeoinstructions text,
    instr_eeolable text,
    instr_wotc text,
    job_app_guid text,
    job_offer_date text,
    job_rate_of_pay text,
    job_start_date timestamp,
    known_as text,
    last_modified_by_proxy text,
    last_modified_date_time timestamp,
    last_name text,
    med_date timestamp,
    med_loc text,
    med_time text,
    middle_name text,
    non_applicant_status text,
    owner text,
    ownership_date timestamp,
    phone_screen_date timestamp,
    phone_screen_details text,
    please_select text,
    pos_title text,
    profile_updated text,
    psych_test_date text,
    psych_test_time text,
    rating text,
    reasonable_accomodation text,
    reference text,
    reference_comments text,
    resume_upload_date timestamp,
    screen_date text,
    snap_shot_date timestamp,
    "source" text,
    source_label text,
    ssn text,
    start_date timestamp,
    state_other text,
    "status" text,
    status_comments text,
    time_to_hire double precision,
    title text,
    user_system_id text,
    visa_num text,
    zip text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (app_status_set_id) REFERENCES job_application_status(id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id),
    FOREIGN KEY (country_code) REFERENCES country(id),
    FOREIGN KEY (job_code) REFERENCES employee_job(job_code),
    FOREIGN KEY (job_req_id) REFERENCES job_requisition(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (referred_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE external_time_data (
    id text,
    created_by text,
    last_modified_by text,
    user_id text,
    category text,
    cost_center text,
    created_date_time timestamp,
    delete_external_time boolean,
    end_date timestamp,
    end_time text,
    hours text,
    last_modified_date_time timestamp,
    logical_date timestamp,
    start_date timestamp,
    start_time text,
    "status" text,
    system_record_status text,
    time_type text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE job_requisition_locale (
    id text,
    job_req_id text,
    external_job_desc_footer text,
    external_job_desc_header text,
    external_job_description text,
    external_title text,
    internal_job_desc_footer text,
    internal_job_desc_header text,
    job_description text,
    job_title text,
    locale text,
    "status" text,
    template_header_footer text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (job_req_id) REFERENCES job_requisition(id)
);

CREATE TABLE job_offer_approver (
    id text,
    created_by text,
    last_modified_by text,
    offer_approval_id text,
    offer_approver_id text,
    approval_step_id text,
    approver_action text,
    approver_action_date timestamp,
    approver_first_name text,
    approver_last_name text,
    approver_order text,
    "comment" text,
    created_date timestamp,
    last_modified_date timestamp,
    username text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (offer_approval_id) REFERENCES job_offer(id),
    FOREIGN KEY (offer_approver_id) REFERENCES users(id)
);

CREATE TABLE fo_division (
    external_code text,
    start_date timestamp,
    created_by text,
    last_modified_by text,
    business_unit_flx text,
    created_date_time timestamp,
    created_on timestamp,
    description text,
    description_de_de text,
    description_default_value text,
    description_en_gb text,
    description_en_us text,
    description_es_es text,
    description_fr_fr text,
    description_ja_jp text,
    description_ko_kr text,
    description_localized text,
    description_nl_nl text,
    description_pt_br text,
    description_pt_pt text,
    description_ru_ru text,
    description_zh_cn text,
    description_zh_tw text,
    end_date timestamp,
    head_of_unit text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    "name" text,
    name_de_de text,
    name_default_value text,
    name_en_gb text,
    name_en_us text,
    name_es_es text,
    name_fr_fr text,
    name_ja_jp text,
    name_ko_kr text,
    name_localized text,
    name_nl_nl text,
    name_pt_br text,
    name_pt_pt text,
    name_ru_ru text,
    name_zh_cn text,
    name_zh_tw text,
    parent text,
    "status" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE employee_time_calendar (
    employee_time_id text,
    id text,
    created_by text,
    last_modified_by text,
    work_schedule_internal_id text,
    created_date timestamp,
    created_date_time timestamp,
    "date" timestamp,
    deduction_quantity text,
    end_time text,
    last_modified_date_time timestamp,
    quantity_in_days text,
    quantity_in_hours text,
    start_time text,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (employee_time_id, id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (work_schedule_internal_id) REFERENCES work_schedule(id)
);

CREATE TABLE pick_list_value (
    id text,
    pick_list_v_2_effective_start_date timestamp,
    pick_list_v_2_id text,
    created_by text,
    last_modified_by text,
    parent_pick_list_value text,
    created_date timestamp,
    created_date_time timestamp,
    l_value text,
    label_default_value text,
    label_localized text,
    last_modified_date_time timestamp,
    legacy_status text,
    max_val text,
    min_val text,
    non_unique_external_code text,
    opt_value text,
    option_id text,
    r_value text,
    "status" text,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, pick_list_v_2_effective_start_date, pick_list_v_2_id),
    FOREIGN KEY (created_by) REFERENCES job_application(id),
    FOREIGN KEY (last_modified_by) REFERENCES job_application(id),
    FOREIGN KEY (parent_pick_list_value) REFERENCES pick_list_value(id)
);

CREATE TABLE holiday_calendar (
    id text,
    created_by text,
    last_modified_by text,
    nominee_user_id text,
    country text,
    created_date timestamp,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    name_default_value text,
    name_localized text,
    old_name text,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (nominee_user_id) REFERENCES users(id)
);

CREATE TABLE country (
    effective_start_date timestamp,
    id text,
    created_by text,
    currency text,
    last_modified_by text,
    created_date_time timestamp,
    effective_end_date timestamp,
    external_name_default_value text,
    external_name_localized text,
    is_drm_enabled boolean,
    is_set_by_migrate boolean,
    last_modified_date_time timestamp,
    numeric_country_code text,
    "status" text,
    system_created_by text,
    system_created_date timestamp,
    system_entity_id text,
    system_last_modified_by text,
    system_last_modified_date timestamp,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_transaction_sequence text,
    system_version_id text,
    territory_id text,
    two_char_country_code text,
    -- custom_* (dynamic column),
    PRIMARY KEY (effective_start_date, id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (currency) REFERENCES currency(code),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE background_languages (
    background_element_id text,
    user_id text,
    background_order_position text,
    language_id text,
    last_modified_date timestamp,
    reading_prof text,
    speaking_prof text,
    variant text,
    writing_prof text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE job_application_onboarding (
    id text,
    application_id text,
    submitted_by text,
    "status" text,
    submitted_on timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (application_id) REFERENCES job_application(id),
    FOREIGN KEY (submitted_by) REFERENCES users(id)
);

CREATE TABLE form_route_step (
    form_data_id text,
    step_order integer,
    completed boolean,
    "current" boolean,
    due_date text,
    over_due boolean,
    stage_name text,
    stage_type text,
    step_description text,
    step_name text,
    user_full_name text,
    user_role text,
    -- custom_* (dynamic column),
    PRIMARY KEY (form_data_id, step_order)
);

CREATE TABLE team_goal_owner (
    id text,
    created_by text,
    last_modified_by text,
    user_id text,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    obj_id text,
    owner_type text,
    system_created_by text,
    system_created_date timestamp,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_last_modified_by text,
    system_last_modified_date timestamp,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE candidate_comments (
    id text,
    candidate_id text,
    associated_comment_id text,
    associated_id text,
    commentator text,
    content text,
    has_associated_comment text,
    migrated_commentator_user_name text,
    ref_type text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id)
);

CREATE TABLE background_check_request (
    id text,
    application_id text,
    created_by text,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    order_status text,
    response_code text,
    response_detail text,
    vendor_code text,
    vendor_order_no text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (application_id) REFERENCES job_application(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE TABLE position_right_to_return (
    id text,
    created_by text,
    "position" text,
    user_id text,
    created_date_time timestamp,
    effective_status text,
    end_date timestamp,
    ga_end_job_info_id text,
    ga_event_reason text,
    ga_start_job_info_id text,
    last_modified_by text,
    last_modified_date_time timestamp,
    loa_time_type text,
    pos_tbhend_row_id text,
    pos_tbhstart_row_id text,
    reason text,
    root_object_id text,
    start_date timestamp,
    system_record_status text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY ("position") REFERENCES "position"(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE employee_beneficiary (
    user_id text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    end_date timestamp,
    last_modified_date_time timestamp,
    payroll_end_date timestamp,
    person_id_external text,
    planned_end_date timestamp,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE employee_pension_payout (
    user_id text,
    created_by text,
    last_modified_by text,
    person_id_external text,
    created_date_time timestamp,
    end_date timestamp,
    last_modified_date_time timestamp,
    payroll_end_date timestamp,
    planned_end_date timestamp,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (person_id_external) REFERENCES external_personal_info(id)
);

CREATE TABLE onboarding_process_task (
    task_id text,
    process_id text,
    created_by text,
    last_modified_by text,
    "comment" text,
    completed_by text,
    created_date_time timestamp,
    end_date timestamp,
    extension_task_ref_id text,
    last_modified_date_time timestamp,
    mdf_system_record_status text,
    responsibility_config text,
    start_date timestamp,
    "status" text,
    task_due_date timestamp,
    "type" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (task_id, process_id),
    FOREIGN KEY (process_id) REFERENCES onboarding_process(process_id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE employee_time_sheet_entry (
    id text,
    employee_time_sheet_id text,
    created_by text,
    last_modified_by text,
    cost_center text,
    created_date timestamp,
    created_date_time timestamp,
    employee_time text,
    end_time text,
    last_modified_date_time timestamp,
    quantity_in_hours text,
    start_date timestamp,
    start_time text,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    time_type_name text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, employee_time_sheet_id),
    FOREIGN KEY (employee_time_sheet_id) REFERENCES employee_time_sheet(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE pick_list (
    _fivetran_id text,
    created_by text,
    last_modified_by text,
    parent_pick_list_nav text,
    created_date timestamp,
    created_date_time timestamp,
    display_order text,
    effective_end_date timestamp,
    effective_start_date timestamp,
    id text,
    last_modified_date_time timestamp,
    legacy_pick_list_id text,
    "name" text,
    parent_pick_list text,
    picklist_creation_source text,
    "status" text,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (_fivetran_id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (parent_pick_list_nav) REFERENCES pick_list(id)
);

CREATE TABLE candidate_background_talent_pool (
    background_element_id text,
    background_order_position text,
    candidate_id text,
    last_modified_date_time timestamp,
    start_date timestamp,
    talent_pool_comments text,
    talent_pool_item text,
    talent_pool_status text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, background_order_position, candidate_id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id)
);

CREATE TABLE employee_time (
    id text,
    created_by text,
    last_modified_by text,
    user_id text,
    workflow_request_id text,
    approval_status text,
    cancellation_workflow_request_id text,
    "comment" text,
    created_date timestamp,
    created_date_time timestamp,
    cust_deliverytype text,
    cust_expectedweek text,
    cust_notificationdate text,
    deduction_quantity text,
    editable boolean,
    end_date timestamp,
    end_time text,
    flexible_requesting boolean,
    fraction_quantity text,
    last_modified_date_time timestamp,
    loa_actual_return_date timestamp,
    loa_end_job_info_id text,
    loa_expected_return_date timestamp,
    loa_start_job_info_id text,
    original_quantity_in_days text,
    quantity_in_days text,
    quantity_in_hours text,
    recurrence_group text,
    start_date timestamp,
    start_time text,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    time_record_origin text,
    time_type text,
    undetermined_end_date boolean,
    workflow_initiated_by_admin boolean,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (workflow_request_id) REFERENCES workflow_request(id)
);

CREATE TABLE job_requisition (
    id text,
    job_code text,
    last_modified_by text,
    status_set_id text,
    accommodation_cost text,
    age text,
    agency_fee text,
    annual_cash_pay text,
    annual_fb text,
    annual_gratuity text,
    annual_pf text,
    annual_retirals text,
    annual_sa text,
    app_template_id text,
    assess_rating_scale_name text,
    candidate_progress text,
    city text,
    classification_time text,
    classification_type text,
    closed_date_time timestamp,
    "comment" text,
    comment_equip text,
    commission text,
    corporate_posting boolean,
    cost_center_id text,
    cost_of_hire text,
    country text,
    created_date_time timestamp,
    currency text,
    default_language text,
    deleted text,
    department text,
    division text,
    erp_amount double precision,
    facility text,
    form_data_id text,
    form_due_date timestamp,
    "function" text,
    hiring_manager_note text,
    industry text,
    instr_cost_hire text,
    instr_equest text,
    instr_equipment text,
    instr_ers text,
    instr_interview text,
    instr_involved_parties text,
    instr_managernote text,
    instr_overall_comments text,
    instr_pd text,
    instr_rec_cost text,
    instr_req text,
    instr_req_dates text,
    instr_req_details text,
    instr_req_justify text,
    instr_req_require text,
    instr_salary text,
    internal_status text,
    intranet_posting boolean,
    is_draft boolean,
    job_req_guid text,
    job_role text,
    job_start_date timestamp,
    last_modified_date_time timestamp,
    last_modified_proxy_user_id text,
    legentity text,
    location text,
    misc_cost text,
    monthly_cash_pay text,
    monthly_fb text,
    monthly_gratuity text,
    monthly_pf text,
    monthly_retirals text,
    monthly_sa text,
    monthly_salary text,
    number_openings text,
    openings_filled text,
    other_bonus text,
    other_comms text,
    other_compensation text,
    other_equip text,
    overall_scale_name text,
    position_number text,
    postalcode text,
    rated_applicant_count text,
    recruit_just text,
    replacement_for_whom text,
    restore_hiring_manager_team_admin_defaults boolean,
    restore_recruiter_team_admin_defaults boolean,
    restore_sourcer_team_admin_defaults boolean,
    restore_vp_of_staffing_team_admin_defaults boolean,
    reverse_scale text,
    sal_rate_type text,
    salary_base text,
    salary_comment text,
    salary_max text,
    salary_min text,
    state_province text,
    stock_package text,
    target_bonus_amount double precision,
    temp_date timestamp,
    template_id text,
    template_type text,
    time_to_fill text,
    total_earnings text,
    total_fixed_pay text,
    total_hire_cost text,
    travel_cost text,
    work_hours text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (job_code) REFERENCES employee_job(job_code),
    FOREIGN KEY (last_modified_by) REFERENCES candidate(id),
    FOREIGN KEY (status_set_id) REFERENCES job_application_status(id)
);

CREATE TABLE employee_job (
    seq_number text,
    start_date timestamp,
    user_id text,
    created_by text,
    last_modified_by text,
    manager_id text,
    assedic_cert_initial_state_num text,
    assedic_cert_object_num text,
    business_unit text,
    company text,
    contract_reference_for_aed text,
    contract_type text,
    cost_center text,
    country_of_company text,
    created_date_time timestamp,
    default_overtime_compensation_variant text,
    department text,
    division text,
    eeo_1_job_category text,
    eeo_4_job_category text,
    eeo_5_job_category text,
    eeo_6_job_category text,
    eeo_class text,
    electoral_college_for_workers_representatives text,
    electoral_college_for_works_council text,
    emp_relationship text,
    empl_status text,
    employee_class text,
    employee_workgroup_membership text,
    employment_type text,
    end_date timestamp,
    event text,
    event_reason text,
    excl_executive_sector boolean,
    family_relationship_with_employer text,
    fgts_date timestamp,
    fgts_percent double precision,
    flsa_status text,
    fte double precision,
    harmful_agent_exposure text,
    health_risk boolean,
    holiday_calendar_code text,
    is_competition_clause_active boolean,
    is_fulltime_employee boolean,
    is_side_line_job_allowed boolean,
    job_code text,
    job_title text,
    labor_protection boolean,
    last_modified_date_time timestamp,
    location text,
    municipal_insee_code text,
    notes text,
    pay_grade text,
    pay_scale_area text,
    pay_scale_group text,
    pay_scale_level text,
    pay_scale_type text,
    "position" text,
    position_entry_date timestamp,
    probation_period_end_date timestamp,
    regular_temp text,
    resident_vote boolean,
    sick_pay_supplement text,
    standard_hours double precision,
    teachers_pension boolean,
    time_recording_admissibility_code text,
    time_recording_profile_code text,
    time_recording_variant text,
    time_type_profile_code text,
    timezone text,
    travel_distance double precision,
    work_location text,
    worker_category text,
    working_days_per_week double precision,
    workschedule_code text,
    -- custom_* (dynamic column),
    PRIMARY KEY (seq_number, start_date, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (manager_id) REFERENCES users(id)
);

CREATE TABLE users (
    id text,
    competency double precision,
    country text,
    emp_id text,
    hr_id text,
    job_code text,
    manager_id text,
    onboarding_id text,
    address_line_1 text,
    address_line_2 text,
    address_line_3 text,
    assignment_uuid text,
    bench_strength text,
    bonus_budget_amount double precision,
    bonus_target double precision,
    business_phone text,
    business_segment text,
    cell_phone text,
    citizenship text,
    city text,
    critical_talent_comments text,
    date_of_birth timestamp,
    date_of_current_position text,
    date_of_position text,
    default_full_name text,
    default_locale text,
    department text,
    display_name text,
    division text,
    email text,
    employee_class text,
    ethnicity text,
    fax text,
    final_job_code text,
    final_job_family text,
    final_job_role text,
    first_name text,
    future_leader boolean,
    gender text,
    hire_date timestamp,
    home_phone text,
    impact_of_loss text,
    impact_of_loss_comments text,
    issue_comments text,
    job_family text,
    job_level text,
    job_role text,
    job_title text,
    key_position boolean,
    last_modified_date_time timestamp,
    last_name text,
    last_review_date timestamp,
    "level" text,
    local_currency_code text,
    location text,
    lumpsum_2_target double precision,
    lumpsum_target double precision,
    married boolean,
    matrix_1_label text,
    matrix_2_label text,
    matrix_managed boolean,
    merit_effective_date timestamp,
    merit_target text,
    mi text,
    minority boolean,
    nationality text,
    new_to_position boolean,
    nickname text,
    objective double precision,
    orig_hire_date timestamp,
    pay_grade text,
    performance double precision,
    potential double precision,
    promotion_amount double precision,
    raise_prorating double precision,
    reason_for_leaving text,
    relo_comments text,
    relo_location text,
    relo_willing text,
    review_freq text,
    risk_of_loss text,
    salary double precision,
    salary_budget_extra_2_percentage double precision,
    salary_budget_extra_percentage double precision,
    salary_budget_final_salary_percentage double precision,
    salary_budget_lumpsum_percentage double precision,
    salary_budget_merit_percentage double precision,
    salary_budget_promotion_percentage double precision,
    salary_budget_total_raise_percentage double precision,
    salary_local double precision,
    salary_prorating double precision,
    salutation text,
    sci_last_modified timestamp,
    seating_chart text,
    service_date timestamp,
    ssn text,
    "state" text,
    "status" text,
    stock_budget_option_amount double precision,
    stock_budget_other_1_amount double precision,
    stock_budget_other_2_amount double precision,
    stock_budget_other_3_amount double precision,
    stock_budget_stock_amount double precision,
    stock_budget_unit_amount double precision,
    suffix text,
    system_cost_of_source double precision,
    system_source text,
    system_starting_salary double precision,
    time_zone text,
    title text,
    username text,
    veteran_disabled boolean,
    veteran_medal boolean,
    veteran_protected boolean,
    veteran_separated boolean,
    zip_code text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (competency) REFERENCES competency_rating(id),
    FOREIGN KEY (country) REFERENCES country(id),
    FOREIGN KEY (emp_id) REFERENCES employment(id),
    FOREIGN KEY (hr_id) REFERENCES users(id),
    FOREIGN KEY (job_code) REFERENCES employee_job(job_code),
    FOREIGN KEY (manager_id) REFERENCES users(id),
    FOREIGN KEY (onboarding_id) REFERENCES job_application_onboarding(id)
);

CREATE TABLE fo_business_unit (
    external_code text,
    start_date timestamp,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    created_on timestamp,
    description text,
    description_de_de text,
    description_default_value text,
    description_en_gb text,
    description_en_us text,
    description_es_es text,
    description_fr_fr text,
    description_ja_jp text,
    description_ko_kr text,
    description_localized text,
    description_nl_nl text,
    description_pt_br text,
    description_pt_pt text,
    description_ru_ru text,
    description_zh_cn text,
    description_zh_tw text,
    end_date timestamp,
    head_of_unit text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    "name" text,
    name_de_de text,
    name_default_value text,
    name_en_gb text,
    name_en_us text,
    name_es_es text,
    name_fr_fr text,
    name_ja_jp text,
    name_ko_kr text,
    name_localized text,
    name_nl_nl text,
    name_pt_br text,
    name_pt_pt text,
    name_ru_ru text,
    name_zh_cn text,
    name_zh_tw text,
    "status" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE interview_individual_assessment (
    id text,
    interview_overall_assessment_id text,
    "comment" text,
    is_deleted text,
    rating text,
    ref_id text,
    "type" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (interview_overall_assessment_id) REFERENCES interview_overall_assessment(id)
);

CREATE TABLE job_application_status (
    id text,
    app_status_id text,
    app_status_name text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (app_status_id) REFERENCES job_application_status_label(id)
);

CREATE TABLE "position" (
    effective_start_date timestamp,
    id text,
    created_by text,
    job_code text,
    last_modified_by text,
    legacy_position_id text,
    business_unit text,
    change_reason text,
    "comment" text,
    company text,
    cost_center text,
    created_date timestamp,
    created_date_time timestamp,
    creation_source text,
    criticality text,
    department text,
    description text,
    division text,
    effective_end_date timestamp,
    effective_status text,
    employee_class text,
    external_name_default_value text,
    external_name_localized text,
    incumbent text,
    job_level text,
    job_title text,
    last_modified_date_time timestamp,
    location text,
    multiple_incumbents_allowed boolean,
    parent_position text,
    pay_grade text,
    pay_range text,
    position_controlled boolean,
    position_criticality text,
    position_title text,
    regular_temporary text,
    standard_hours text,
    system_entity_id text,
    system_object_type text,
    system_optimistic_lock_uuid text,
    system_record_id text,
    system_record_status text,
    system_version_id text,
    target_fte text,
    technical_parameters text,
    transaction_sequence text,
    "type" text,
    vacant boolean,
    -- custom_* (dynamic column),
    PRIMARY KEY (effective_start_date, id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (job_code) REFERENCES employee_job(job_code),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (legacy_position_id) REFERENCES "position"(id)
);

CREATE TABLE background_compensation (
    background_element_id text,
    user_id text,
    background_order_position text,
    comparison_ratio double precision,
    cur_salary text,
    fin_salary text,
    job_title text,
    last_modified_date timestamp,
    lump_sum double precision,
    merit text,
    options text,
    pm_rating text,
    stock text,
    system_comp_template_name text,
    system_review_end_date timestamp,
    system_review_start_date timestamp,
    total_comp text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE candidate_background_certificate (
    background_element_id text,
    candidate_id text,
    background_order_position text,
    description text,
    end_date timestamp,
    institution text,
    last_modified_date_time timestamp,
    license_country text,
    license_name text,
    license_number text,
    license_state text,
    license_type text,
    "name" text,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id)
);

CREATE TABLE background_talent_pool_corp (
    background_element_id text,
    user_id text,
    talent_pool_item_corp text,
    talent_pool_status text,
    background_order_position text,
    last_modified_date timestamp,
    start_date timestamp,
    talent_pool_comments text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (talent_pool_item_corp) REFERENCES pick_list_option(id),
    FOREIGN KEY (talent_pool_status) REFERENCES pick_list_option(id)
);

CREATE TABLE fo_location (
    external_code text,
    start_date text,
    created_by text,
    last_modified_by text,
    created_date_time text,
    created_on text,
    description text,
    description_de_de text,
    description_default_value text,
    description_en_gb text,
    description_en_us text,
    description_es_es text,
    description_fr_fr text,
    description_ja_jp text,
    description_ko_kr text,
    description_localized text,
    description_nl_nl text,
    description_pt_br text,
    description_pt_pt text,
    description_ru_ru text,
    description_zh_cn text,
    description_zh_tw text,
    end_date text,
    head_of_unit text,
    last_modified_date_time text,
    last_modified_on text,
    "name" text,
    name_de_de text,
    name_default_value text,
    name_en_gb text,
    name_en_us text,
    name_es_es text,
    name_fr_fr text,
    name_ja_jp text,
    name_ko_kr text,
    name_localized text,
    name_nl_nl text,
    name_pt_br text,
    name_pt_pt text,
    name_ru_ru text,
    name_zh_cn text,
    name_zh_tw text,
    "status" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE vendor_info (
    effective_start_date timestamp,
    id text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    description_default_value text,
    description_localized text,
    effective_status text,
    last_modified_date_time timestamp,
    system_created_by text,
    system_created_date timestamp,
    system_effective_end_date timestamp,
    system_entity_id text,
    system_last_modified_by text,
    system_last_modified_date timestamp,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_transaction_sequence text,
    system_version_id text,
    vendor_name text,
    -- custom_* (dynamic column),
    PRIMARY KEY (effective_start_date, id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE fo_pay_grade (
    external_code text,
    start_date timestamp,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    created_on timestamp,
    description text,
    end_date timestamp,
    internal_code text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    "name" text,
    object_id text,
    paygrade_level text,
    "status" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE job_requisition_question (
    id text,
    question_category text,
    question_name text,
    question_source text,
    question_type text,
    PRIMARY KEY (id)
);

CREATE TABLE per_global_info_fra (
    country text,
    start_date timestamp,
    person_id_external text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    created_on timestamp,
    end_date timestamp,
    generic_date_1 timestamp,
    generic_date_10 timestamp,
    generic_date_2 timestamp,
    generic_date_3 timestamp,
    generic_date_4 timestamp,
    generic_date_5 timestamp,
    generic_date_6 timestamp,
    generic_date_7 timestamp,
    generic_number_1 text,
    generic_number_10 text,
    generic_number_11 text,
    generic_number_12 text,
    generic_number_13 text,
    generic_number_14 text,
    generic_number_15 text,
    generic_number_16 text,
    generic_number_2 text,
    generic_number_3 text,
    generic_number_4 text,
    generic_number_5 text,
    generic_number_6 text,
    generic_number_7 text,
    generic_number_8 text,
    generic_number_9 text,
    generic_string_1 text,
    generic_string_10 text,
    generic_string_11 text,
    generic_string_12 text,
    generic_string_13 text,
    generic_string_14 text,
    generic_string_15 text,
    generic_string_16 text,
    generic_string_17 text,
    generic_string_18 text,
    generic_string_19 text,
    generic_string_2 text,
    generic_string_20 text,
    generic_string_21 text,
    generic_string_22 text,
    generic_string_3 text,
    generic_string_4 text,
    generic_string_5 text,
    generic_string_6 text,
    generic_string_7 text,
    generic_string_8 text,
    generic_string_9 text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    operation text,
    -- custom_* (dynamic column),
    PRIMARY KEY (country, start_date, person_id_external),
    FOREIGN KEY (person_id_external) REFERENCES per_person(person_id_external),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE background_courses (
    background_element_id text,
    user_id text,
    instruction_type text,
    background_order_position text,
    course text,
    end_date timestamp,
    institution text,
    last_modified_date timestamp,
    length text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (instruction_type) REFERENCES pick_list_option(id)
);

CREATE TABLE job_requisition_group_operator (
    operator_role text,
    user_group_id text,
    job_req_id text,
    admin_selected_group_to_be_removed boolean,
    is_admin_selected boolean,
    is_disabled boolean,
    user_group_name text,
    -- custom_* (dynamic column),
    PRIMARY KEY (operator_role, user_group_id, job_req_id),
    FOREIGN KEY (job_req_id) REFERENCES job_requisition(id)
);

CREATE TABLE candidate (
    id text,
    user_id text,
    address text,
    address_2 text,
    agree_to_privacy_statement text,
    anonymized text,
    anonymized_date_time timestamp,
    candidate_locale text,
    cell_phone text,
    city text,
    contact_email text,
    country text,
    creation_date_time timestamp,
    current_title text,
    data_privacy_id text,
    dateof_availability text,
    external_candidate boolean,
    first_name text,
    home_phone text,
    last_login_date_time timestamp,
    last_modified_date_time timestamp,
    last_name text,
    middle_name text,
    partner_member_id text,
    partner_source text,
    password text,
    primary_email text,
    privacy_accept_date_time timestamp,
    public_intranet boolean,
    share_profile text,
    visibility_option boolean,
    zip text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE candidate_profile_extension (
    id text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    system_record_status text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (id) REFERENCES candidate(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE candidate_background_outside_work_experience (
    background_element_id text,
    candidate_id text,
    employer_state text,
    background_order_position text,
    business_type text,
    employer text,
    employer_address text,
    employer_city text,
    employer_contact text,
    employer_country text,
    employer_email text,
    employer_phone text,
    employer_zip text,
    end_date timestamp,
    last_modified_date_time timestamp,
    present_employer text,
    start_date timestamp,
    start_title text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id),
    FOREIGN KEY (employer_state) REFERENCES pick_list_option(id)
);

CREATE TABLE external_time_segment (
    id text,
    external_time_record_id text,
    created_by text,
    last_modified_by text,
    created_date timestamp,
    created_date_time timestamp,
    hours text,
    last_modified_date_time timestamp,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, external_time_record_id),
    FOREIGN KEY (external_time_record_id) REFERENCES external_time_record(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE per_email (
    email_type text,
    person_id_external text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    created_on timestamp,
    email_address text,
    include_all_records boolean,
    is_primary boolean,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    operation text,
    -- custom_* (dynamic column),
    PRIMARY KEY (email_type, person_id_external),
    FOREIGN KEY (person_id_external) REFERENCES per_person(person_id_external),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE background_special_assign (
    background_element_id text,
    user_id text,
    background_order_position text,
    description text,
    end_date timestamp,
    last_modified_date timestamp,
    project text,
    start_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES pick_list_option(id)
);

CREATE TABLE job_application_comments (
    id text,
    application_id text,
    associated_comment_id text,
    last_modified_by text,
    user_id text,
    associated_id text,
    commentator text,
    content text,
    has_associated_comment text,
    migrated_commentator_user_name text,
    ref_type text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (application_id) REFERENCES job_application(id),
    FOREIGN KEY (associated_comment_id) REFERENCES job_application_comments(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE job_offer (
    id text,
    created_by text,
    last_modified_by text,
    annual_cash_pay text,
    annual_fb text,
    annual_gratuity text,
    annual_pf text,
    annual_retirals text,
    annual_sa text,
    anonymized_date timestamp,
    anonymized_flag text,
    application_id text,
    cand_just text,
    candidate_name text,
    commission text,
    created_date timestamp,
    currency text,
    current_location text,
    form_data_id text,
    form_template_id text,
    initial_comment text,
    internal_status text,
    job_rate_of_pay text,
    last_modified_date timestamp,
    location text,
    monthly_cash_pay text,
    monthly_fb text,
    monthly_gratuity text,
    monthly_pf text,
    monthly_retirals text,
    monthly_sa text,
    monthly_salary text,
    redefine_template_approvers boolean,
    salary_base text,
    stock_package text,
    target_bonus_amount double precision,
    temp_date timestamp,
    template_id text,
    total_earnings text,
    total_fixed_pay text,
    version text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE holiday_category (
    id text,
    created_by text,
    last_modified_by text,
    amount text,
    created_date timestamp,
    created_date_time timestamp,
    employee_payroll_run_results_external_code text,
    employee_payroll_run_results_system_effective_start_date timestamp,
    end_date_when_earned timestamp,
    external_name text,
    grouping_reason text,
    last_modified_date_time timestamp,
    payroll_provider_grouping_reason text,
    payroll_provider_grouping_value text,
    payroll_provider_unit_of_measurement text,
    payroll_provider_wage_type text,
    quantity text,
    start_date_when_earned timestamp,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    unit_of_measurement text,
    wage_type text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE payment_information_detail (
    effective_start_date timestamp,
    external_code text,
    worker_id text,
    created_by text,
    last_modified_by text,
    account_number text,
    account_owner text,
    amount text,
    bank text,
    bank_country text,
    business_identifier_code text,
    created_date timestamp,
    created_date_time timestamp,
    currency text,
    iban text,
    last_modified_date timestamp,
    last_modified_date_time timestamp,
    last_modified_date_with_tz timestamp,
    mdf_system_effective_end_date timestamp,
    mdf_system_effective_start_date timestamp,
    mdf_system_entity_id text,
    mdf_system_object_type text,
    mdf_system_record_id text,
    mdf_system_record_status text,
    mdf_system_status text,
    mdf_system_transaction_sequence text,
    mdf_system_version_id text,
    pay_sequence text,
    pay_type text,
    payment_method text,
    percent text,
    purpose text,
    routing_number text,
    -- custom_* (dynamic column),
    PRIMARY KEY (effective_start_date, external_code, worker_id),
    FOREIGN KEY (worker_id) REFERENCES employment(user_id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE fo_cost_center (
    external_code text,
    start_date text,
    created_by text,
    last_modified_by text,
    cost_center_external_object_id text,
    cost_center_manager text,
    created_date_time text,
    created_on text,
    cust_legal_entity_prop text,
    description text,
    description_de_de text,
    description_default_value text,
    description_en_gb text,
    description_en_us text,
    description_es_es text,
    description_fr_fr text,
    description_ja_jp text,
    description_ko_kr text,
    description_localized text,
    description_nl_nl text,
    description_pt_br text,
    description_pt_pt text,
    description_ru_ru text,
    description_zh_cn text,
    description_zh_tw text,
    end_date text,
    gl_statement_code text,
    last_modified_date_time text,
    last_modified_on text,
    "name" text,
    name_de_de text,
    name_default_value text,
    name_en_gb text,
    name_en_us text,
    name_es_es text,
    name_fr_fr text,
    name_ja_jp text,
    name_ko_kr text,
    name_localized text,
    name_nl_nl text,
    name_pt_br text,
    name_pt_pt text,
    name_ru_ru text,
    name_zh_cn text,
    name_zh_tw text,
    parent text,
    "status" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE employee_time_sheet (
    id text,
    created_by text,
    last_modified_by text,
    user_id text,
    workflow_request_id text,
    approval_status text,
    "comment" text,
    created_date timestamp,
    created_date_time timestamp,
    editable boolean,
    end_date timestamp,
    last_modified_date_time timestamp,
    planned_working_time text,
    recorded_working_time text,
    replaced_by text,
    replaced_by_nav text,
    replaces text,
    replaces_nav text,
    start_date timestamp,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    workflow_action text,
    working_time_account text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (workflow_request_id) REFERENCES workflow_request(id)
);

CREATE TABLE background_preferred_next_move (
    background_element_id text,
    user_id text,
    last_modified_by text,
    to_currency text,
    background_order_position text,
    comments text,
    "function" text,
    last_modified_date timestamp,
    "level" text,
    timeframe text,
    title text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (to_currency) REFERENCES currency(code)
);

CREATE TABLE interview_overall_assessment (
    id text,
    interview_ref_id text,
    average_rating double precision,
    comments text,
    overall_rating text,
    "type" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (interview_ref_id) REFERENCES job_application_interview(id)
);

CREATE TABLE payroll_run_results_item (
    external_id text,
    payroll_run_results_system_effective_start_date timestamp,
    payroll_run_results_external_id text,
    created_by text,
    last_modified_by text,
    amount double precision,
    created_date timestamp,
    created_date_time timestamp,
    end_date_when_earned timestamp,
    external_name text,
    grouping_reason text,
    last_modified_date_time timestamp,
    payroll_provider_grouping_reason text,
    payroll_provider_grouping_value text,
    payroll_provider_unit_of_measurement text,
    payroll_provider_wage_type text,
    quantity text,
    start_date_when_earned timestamp,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    unit_of_measurement text,
    wage_type text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_id, payroll_run_results_system_effective_start_date, payroll_run_results_external_id),
    FOREIGN KEY (payroll_run_results_external_id) REFERENCES payroll_run_results(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE candidate_background_language (
    background_element_id text,
    candidate_id text,
    background_order_position text,
    languages text,
    last_modified_date_time timestamp,
    reading_prof text,
    speaking_prof text,
    writing_prof text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id),
    FOREIGN KEY (candidate_id) REFERENCES candidate(id)
);

CREATE TABLE payroll_run_results (
    id text,
    system_effective_start_date timestamp,
    created_by text,
    employment_id text,
    last_modified_by text,
    person_id text,
    user_id text,
    client_id text,
    company_id text,
    created_date timestamp,
    created_date_time timestamp,
    currency text,
    end_date_when_paid timestamp,
    external_name text,
    is_void boolean,
    last_modified_date_time timestamp,
    pay_date timestamp,
    payroll_id text,
    payroll_provider_id text,
    payroll_provider_payroll_run_type text,
    payroll_run_type text,
    sequence_number text,
    start_date_when_paid timestamp,
    system_effective_end_date timestamp,
    system_entity_id text,
    system_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, system_effective_start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (employment_id) REFERENCES employment(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (person_id) REFERENCES external_personal_info(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE employment (
    id text,
    person_id text,
    user_id text,
    created_by text,
    job_number text,
    last_modified_by text,
    assignment_class text,
    benefits_eligibility_start_date timestamp,
    benefits_end_date timestamp,
    bonus_pay_expiration_date timestamp,
    created_date_time timestamp,
    eligible_for_sal_continuation boolean,
    eligible_for_stock boolean,
    employee_first_employment boolean,
    end_date timestamp,
    first_date_worked timestamp,
    include_all_records boolean,
    initial_option_grant double precision,
    initial_stock_grant double precision,
    is_contingent_worker boolean,
    is_ecrecord boolean,
    last_date_worked text,
    last_modified_date_time timestamp,
    ok_to_rehire boolean,
    original_start_date timestamp,
    payroll_end_date timestamp,
    prev_employee_id text,
    professional_service_date timestamp,
    regret_termination boolean,
    salary_end_date timestamp,
    seniority_date timestamp,
    service_date timestamp,
    start_date timestamp,
    stock_end_date timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, person_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (job_number) REFERENCES employee_job(seq_number),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE background_mobility (
    background_element_id text,
    user_id text,
    willingness text,
    background_order_position text,
    last_modified_date timestamp,
    location text,
    -- custom_* (dynamic column),
    PRIMARY KEY (background_element_id, user_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (willingness) REFERENCES pick_list_option(id)
);

CREATE TABLE time_zone (
    effective_start_date timestamp,
    id text,
    country text,
    created_by text,
    last_modified_by text,
    "comment" text,
    created_date_time timestamp,
    effective_status text,
    last_modified_date_time timestamp,
    name_default_value text,
    name_localized text,
    supported boolean,
    system_created_by text,
    system_created_date timestamp,
    system_effective_end_date timestamp,
    system_entity_id text,
    system_last_modified_by text,
    system_last_modified_date timestamp,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_transaction_sequence text,
    system_version_id text,
    utc_dst_offset text,
    utc_offset text,
    -- custom_* (dynamic column),
    PRIMARY KEY (effective_start_date, id),
    FOREIGN KEY (country) REFERENCES country(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE fo_job_code (
    external_code text,
    start_date timestamp,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    created_on timestamp,
    cust_bonus_plan text,
    cust_eng_non text,
    cust_global_jobgrade text,
    default_employee_class text,
    default_job_level text,
    default_supervisor_level text,
    description text,
    description_de_de text,
    description_default_value text,
    description_en_debug text,
    description_en_gb text,
    description_en_us text,
    description_es_es text,
    description_fr_fr text,
    description_ja_jp text,
    description_ko_kr text,
    description_localized text,
    description_nl_nl text,
    description_pt_br text,
    description_pt_pt text,
    description_ru_ru text,
    description_zh_cn text,
    description_zh_tw text,
    employee_class text,
    end_date timestamp,
    entity_oid text,
    entity_uuid text,
    grade text,
    is_fulltime_employee boolean,
    is_regular text,
    job_function text,
    job_level text,
    last_modified_date_time timestamp,
    last_modified_on timestamp,
    mdf_system_record_id text,
    "name" text,
    name_de_de text,
    name_default_value text,
    name_en_debug text,
    name_en_gb text,
    name_en_us text,
    name_es_es text,
    name_fr_fr text,
    name_ja_jp text,
    name_ko_kr text,
    name_localized text,
    name_nl_nl text,
    name_pt_br text,
    name_pt_pt text,
    name_ru_ru text,
    name_zh_cn text,
    name_zh_tw text,
    parent_job_code text,
    regular_temporary text,
    standard_hours double precision,
    "status" text,
    supervisor_level text,
    worker_comp_code text,
    -- custom_* (dynamic column),
    PRIMARY KEY (external_code, start_date),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE job_application_status_audit_trail (
    rev_number text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    last_modified_proxy_user text,
    rev_type integer,
    skipped_status text,
    status_comment text,
    -- custom_* (dynamic column),
    PRIMARY KEY (rev_number),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE employee_time_valuation_result (
    id text,
    employee_time_sheet_id text,
    created_by text,
    last_modified_by text,
    allowance_type text,
    approval_status text,
    booking_date timestamp,
    cost_center text,
    created_date timestamp,
    created_date_time timestamp,
    hours text,
    last_modified_date_time timestamp,
    pay_type_external_name text,
    pay_type_name text,
    posting_target text,
    system_effective_end_date timestamp,
    system_effective_start_date timestamp,
    system_entity_id text,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_status text,
    system_transaction_sequence text,
    system_version_id text,
    time_type_group text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id, employee_time_sheet_id),
    FOREIGN KEY (employee_time_sheet_id) REFERENCES employee_time_sheet(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE successor (
    id text,
    created_by text,
    last_modified_by text,
    nominee_user_id text,
    last_modified_date_time timestamp,
    note text,
    readiness double precision,
    readiness_label text,
    "status" text,
    status_label text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (nominee_user_id) REFERENCES users(id)
);

CREATE TABLE talent_pool (
    effective_start_date timestamp,
    id text,
    created_by text,
    last_modified_by text,
    created_date_time timestamp,
    description_default_value text,
    description_localized text,
    effective_end_date timestamp,
    effective_status text,
    enable_readiness boolean,
    last_modified_date_time timestamp,
    name_default_value text,
    name_localized text,
    owner text,
    system_created_by text,
    system_created_date timestamp,
    system_entity_id text,
    system_last_modified_by text,
    system_last_modified_date timestamp,
    system_object_type text,
    system_record_id text,
    system_record_status text,
    system_version_id text,
    transaction_sequence text,
    "type" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (effective_start_date, id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE competency_rating (
    id text,
    user_id text,
    form_content_id text,
    form_data_id text,
    guid text,
    last_modified timestamp,
    last_modified_date_time timestamp,
    module integer,
    rater text,
    rater_category text,
    rating double precision,
    scale_max text,
    scale_min text,
    "source" integer,
    "status" text,
    "type" integer,
    valid_from timestamp,
    valid_to timestamp,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE pick_list_option (
    id text,
    parent_picklist_option text,
    picklist_id text,
    external_code text,
    max_value double precision,
    min_value double precision,
    option_value double precision,
    sort_order integer,
    "status" text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (parent_picklist_option) REFERENCES pick_list_option(id),
    FOREIGN KEY (picklist_id) REFERENCES pick_list(legacy_pick_list_id)
);

CREATE TABLE employee_work_permit (
    document_number text,
    issue_date timestamp,
    country text,
    document_type text,
    user_id text,
    created_by text,
    last_modified_by text,
    attachment text,
    attachment_file_name text,
    attachment_file_size text,
    attachment_file_type text,
    attachment_id text,
    attachment_mime_type text,
    attachment_status text,
    created_date_time timestamp,
    document_title text,
    expiration_date timestamp,
    is_validated boolean,
    issue_place text,
    issuing_authority text,
    last_modified_date_time timestamp,
    notes text,
    -- custom_* (dynamic column),
    PRIMARY KEY (document_number, issue_date, country, document_type, user_id),
    FOREIGN KEY (country) REFERENCES country(id),
    FOREIGN KEY (document_type) REFERENCES pick_list_option(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id)
);

CREATE TABLE position_requisition_status (
    id text,
    created_by text,
    last_modified_by text,
    position_id text,
    requisition_id text,
    created_date_time timestamp,
    last_modified_date_time timestamp,
    processing_status text,
    system_record_status text,
    -- custom_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (last_modified_by) REFERENCES users(id),
    FOREIGN KEY (position_id) REFERENCES "position"(id),
    FOREIGN KEY (requisition_id) REFERENCES job_requisition(id)
);
