CREATE TABLE country (
    id text,
    active text,
    custom_fields text,
    "name" text,
    three_alpha_code text,
    two_alpha_code text,
    PRIMARY KEY (id)
);

CREATE TABLE booking_line (
    "index" text,
    booking_id text,
    acv_amount text,
    acv_base_currency_amount text,
    acv_base_currency_code text,
    acv_currency_code text,
    acv_currency_conversion_date text,
    charge_description text,
    charge_external_crm_id text,
    charge_external_erp_id text,
    charge_id text,
    charge_name text,
    charge_number text,
    cmrr_amount text,
    cmrr_base_currency_amount text,
    cmrr_base_currency_code text,
    cmrr_currency_code text,
    cmrr_currency_conversion_date text,
    created text,
    emrr_amount text,
    emrr_base_currency_amount text,
    emrr_base_currency_code text,
    emrr_currency_code text,
    emrr_currency_conversion_date text,
    fmrr_amount text,
    fmrr_base_currency_amount text,
    fmrr_base_currency_code text,
    fmrr_currency_code text,
    fmrr_currency_conversion_date text,
    modified text,
    one_time_fees_amount text,
    one_time_fees_base_currency_amount text,
    one_time_fees_base_currency_code text,
    one_time_fees_currency_code text,
    one_time_fees_currency_conversion_date text,
    tcv_amount text,
    tcv_base_currency_amount text,
    tcv_base_currency_code text,
    tcv_currency_code text,
    tcv_currency_conversion_date text,
    PRIMARY KEY ("index", booking_id),
    FOREIGN KEY (booking_id) REFERENCES booking(id)
);

CREATE TABLE booking (
    id text,
    order_id text,
    account_category text,
    booking_type text,
    change_type text,
    classification_chart_color text,
    classification_description text,
    classification_is_system_classification text,
    classification_name text,
    classification_type text,
    created text,
    currency text,
    effective_date text,
    modified text,
    PRIMARY KEY (id),
    FOREIGN KEY (order_id) REFERENCES subscription(id)
);

CREATE TABLE usage (
    id text,
    account_id text,
    order_id text,
    product_id text,
    charge_description text,
    charge_external_crm_id text,
    charge_external_erp_id text,
    charge_id text,
    charge_name text,
    charge_number text,
    created text,
    description text,
    from_date text,
    import_date text,
    modified text,
    processed text,
    product_description text,
    quantity text,
    rated_price text,
    rated_price_currency_code text,
    "source" text,
    to_date text,
    unit text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (account_id) REFERENCES account(id),
    FOREIGN KEY (order_id) REFERENCES salesorder(id),
    FOREIGN KEY (product_id) REFERENCES product(id)
);

CREATE TABLE subscription_product_charge_price_detail (
    "index" text,
    subscription_id text,
    subscription_product_charge_id text,
    subscription_product_id text,
    description text,
    from_quantity text,
    line_discount_amount text,
    line_discount_percent text,
    list_price text,
    price text,
    price_base text,
    tier text,
    to_quantity text,
    PRIMARY KEY ("index", subscription_id, subscription_product_charge_id, subscription_product_id),
    FOREIGN KEY (subscription_id) REFERENCES subscription_product_charge(subscription_id),
    FOREIGN KEY (subscription_product_charge_id) REFERENCES subscription_product_charge(id),
    FOREIGN KEY (subscription_product_id) REFERENCES subscription_product_charge(subscription_product_id)
);

CREATE TABLE subscription_product_charge_feature (
    "index" text,
    subscription_id text,
    subscription_product_charge_id text,
    subscription_product_id text,
    code text,
    description text,
    PRIMARY KEY ("index", subscription_id, subscription_product_charge_id, subscription_product_id),
    FOREIGN KEY (subscription_id) REFERENCES subscription_product_charge(subscription_id),
    FOREIGN KEY (subscription_product_charge_id) REFERENCES subscription_product_charge(id),
    FOREIGN KEY (subscription_product_id) REFERENCES subscription_product_charge(subscription_product_id)
);

CREATE TABLE subscription_product_charge (
    id text,
    subscription_id text,
    subscription_product_id text,
    charge_id text,
    order_id text,
    order_product_id text,
    acv_amount text,
    acv_base_currency_amount text,
    acv_base_currency_code text,
    acv_currency_code text,
    acv_currency_conversion_date text,
    billing_period text,
    change_state text,
    charge_type text,
    charged_through_date text,
    cmrr_amount text,
    cmrr_base_currency_amount text,
    cmrr_base_currency_code text,
    cmrr_currency_code text,
    cmrr_currency_conversion_date text,
    created text,
    display_price text,
    effective_end_date text,
    effective_start_date text,
    emrr_amount text,
    emrr_base_currency_amount text,
    emrr_base_currency_code text,
    emrr_currency_code text,
    emrr_currency_conversion_date text,
    end_on text,
    estimated_quantity text,
    estimated_usage text,
    is_last_version text,
    modified text,
    number text,
    one_time_feesamount text,
    one_time_feesbase_currency_amount text,
    one_time_feesbase_currency_code text,
    one_time_feescurrency_code text,
    one_time_feescurrency_conversion_date text,
    quantity text,
    recurring_monthly_amount text,
    recurring_monthly_amount_base text,
    revenue_recognition_rule text,
    start_on text,
    tcv_amount text,
    tcv_base_currency_amount text,
    tcv_base_currency_code text,
    tcv_currency_code text,
    tcv_currency_conversion_date text,
    version text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id, subscription_id, subscription_product_id),
    FOREIGN KEY (subscription_id) REFERENCES subscription_product(subscription_id),
    FOREIGN KEY (subscription_product_id) REFERENCES subscription_product(id),
    FOREIGN KEY (charge_id) REFERENCES product_charge_plan_detail(id),
    FOREIGN KEY (order_id) REFERENCES subscription(id),
    FOREIGN KEY (order_product_id) REFERENCES subscription_product(id)
);

CREATE TABLE subscription_product (
    id text,
    subscription_id text,
    charge_plan_id text,
    created text,
    external_crm_id text,
    external_erp_id text,
    modified text,
    "name" text,
    product_line_number text,
    product_number text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id, subscription_id),
    FOREIGN KEY (subscription_id) REFERENCES subscription(id),
    FOREIGN KEY (charge_plan_id) REFERENCES product_charge_plan(id)
);

CREATE TABLE subscription_milestone (
    id text,
    subscription_id text,
    description text,
    milestone_date text,
    "name" text,
    order_id text,
    planned_date text,
    PRIMARY KEY (id, subscription_id),
    FOREIGN KEY (subscription_id) REFERENCES subscription(id)
);

CREATE TABLE subscription_order_discount_product_charge (
    "index" text,
    subscription_order_discount_id text,
    subscription_order_discount_subscription_id text,
    charge_id text,
    order_discount_id text,
    PRIMARY KEY ("index", subscription_order_discount_id, subscription_order_discount_subscription_id),
    FOREIGN KEY (subscription_order_discount_id) REFERENCES subscription_order_discount(id),
    FOREIGN KEY (subscription_order_discount_subscription_id) REFERENCES subscription_order_discount(subscription_id)
);

CREATE TABLE subscription_order_discount (
    id text,
    subscription_id text,
    discount_type text,
    end_date text,
    end_on text,
    order_id text,
    percents text,
    start_date text,
    start_on text,
    PRIMARY KEY (id, subscription_id),
    FOREIGN KEY (subscription_id) REFERENCES subscription(id)
);

CREATE TABLE subscription (
    id text,
    account_id text,
    account_external_crm_id text,
    account_external_erp_id text,
    account_name text,
    account_number text,
    buyer_reference text,
    cancellation_date text,
    created text,
    currency text,
    currency_code_to_use_when_invoice text,
    description text,
    effective_change_date text,
    effective_end_date text,
    effective_start_date text,
    external_crm_id text,
    external_erp_id text,
    invoice_accountexternal_crm_id text,
    invoice_accountexternal_erp_id text,
    invoice_accountid text,
    invoice_accountname text,
    invoice_accountnumber text,
    invoice_addresscity text,
    invoice_addresscountry text,
    invoice_addresscounty text,
    invoice_addressdescription text,
    invoice_addressid text,
    invoice_addressstate text,
    invoice_addressstreet text,
    invoice_addressstreet_2 text,
    invoice_addresszip text,
    invoice_batch_groupcode text,
    invoice_batch_groupdescription text,
    invoice_batch_groupid text,
    invoice_separatly text,
    is_auto_renewed text,
    is_last_version text,
    last_renewal_date text,
    modified text,
    notice_period text,
    notice_period_date text,
    order_billing_period text,
    order_date text,
    order_number text,
    order_payment_method text,
    order_type text,
    our_reference text,
    payment_term text,
    remarks text,
    renewal_term text,
    set_order_billing_period text,
    "status" text,
    term text,
    term_type text,
    use_account_invoice_batch_group text,
    version text,
    your_order_number text,
    your_reference text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (account_id) REFERENCES account(id)
);

CREATE TABLE chart_of_account (
    id text,
    code text,
    description text,
    external_crm_id text,
    external_erp_id text,
    "name" text,
    PRIMARY KEY (id)
);

CREATE TABLE salesorder_product_charge_price_detail (
    "index" text,
    salesorder_id text,
    salesorder_product_charge_id text,
    salesorder_product_id text,
    description text,
    from_quantity text,
    line_discount_amount text,
    line_discount_percent text,
    list_price text,
    price text,
    tier text,
    to_quantity text,
    PRIMARY KEY ("index", salesorder_id, salesorder_product_charge_id, salesorder_product_id),
    FOREIGN KEY (salesorder_id) REFERENCES salesorder_product_charge(salesorder_id),
    FOREIGN KEY (salesorder_product_charge_id) REFERENCES salesorder_product_charge(id),
    FOREIGN KEY (salesorder_product_id) REFERENCES salesorder_product_charge(salesorder_product_id)
);

CREATE TABLE salesorder_product_charge_feature (
    "index" text,
    salesorder_id text,
    salesorder_product_charge_id text,
    salesorder_product_id text,
    code text,
    description text,
    PRIMARY KEY ("index", salesorder_id, salesorder_product_charge_id, salesorder_product_id),
    FOREIGN KEY (salesorder_id) REFERENCES salesorder_product_charge(salesorder_id),
    FOREIGN KEY (salesorder_product_charge_id) REFERENCES salesorder_product_charge(id),
    FOREIGN KEY (salesorder_product_id) REFERENCES salesorder_product_charge(salesorder_product_id)
);

CREATE TABLE salesorder_product_charge (
    id text,
    salesorder_id text,
    salesorder_product_id text,
    accounts_receivable_account text,
    acv_amount text,
    acv_base_currency_amount text,
    acv_base_currency_code text,
    acv_currency_code text,
    acv_currency_conversion_date text,
    alignment_date text,
    billing_day text,
    billing_period text,
    billing_timing text,
    change_state text,
    charge_id text,
    charge_number text,
    charge_type text,
    charged_through_date text,
    cmrr_amount text,
    cmrr_base_currency_amount text,
    cmrr_base_currency_code text,
    cmrr_currency_code text,
    cmrr_currency_conversion_date text,
    create_invoice_lines_per_tier text,
    created text,
    deferred_revenue_account text,
    display_price text,
    effective_end_date text,
    effective_start_date text,
    emrr_amount text,
    emrr_base_currency_amount text,
    emrr_base_currency_code text,
    emrr_currency_code text,
    emrr_currency_conversion_date text,
    end_on text,
    estimated_quantity text,
    estimated_usage text,
    external_crm_id text,
    external_erp_id text,
    is_last_version text,
    last_price_adjustment_date text,
    last_renewal_date text,
    modified text,
    "name" text,
    one_time_feesamount text,
    one_time_feesbase_currency_amount text,
    one_time_feesbase_currency_code text,
    one_time_feescurrency_code text,
    one_time_feescurrency_conversion_date text,
    order_id text,
    order_product_id text,
    ordered_quantity text,
    period_alignment text,
    price_model text,
    price_period text,
    quantity text,
    recognized_revenue_account text,
    recurring_monthly_amount text,
    recurring_monthly_amount_base text,
    remaining_quantity text,
    remarks text,
    revenue_recognition_rule text,
    specific_billing_day text,
    start_on text,
    tax_included text,
    tax_template text,
    tcv_amount text,
    tcv_base_currency_amount text,
    tcv_base_currency_code text,
    tcv_currency_code text,
    tcv_currency_conversion_date text,
    unit_code text,
    usage_rating text,
    version text,
    PRIMARY KEY (id, salesorder_id, salesorder_product_id),
    FOREIGN KEY (salesorder_id) REFERENCES salesorder_product(salesorder_id),
    FOREIGN KEY (salesorder_product_id) REFERENCES salesorder_product(id)
);

CREATE TABLE salesorder_product (
    id text,
    salesorder_id text,
    charge_plan_id text,
    created text,
    external_crm_id text,
    external_erp_id text,
    modified text,
    "name" text,
    product_line_number text,
    product_number text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id, salesorder_id),
    FOREIGN KEY (salesorder_id) REFERENCES salesorder(id),
    FOREIGN KEY (charge_plan_id) REFERENCES product_charge_plan(id)
);

CREATE TABLE salesorder_milestone (
    id text,
    salesorder_id text,
    description text,
    milestone_date text,
    "name" text,
    order_id text,
    planned_date text,
    PRIMARY KEY (id, salesorder_id),
    FOREIGN KEY (salesorder_id) REFERENCES salesorder(id)
);

CREATE TABLE salesorder_order_discount_product_charge (
    "index" text,
    salesorder_order_discount_id text,
    salesorder_order_discount_salesorder_id text,
    charge_id text,
    order_discount_id text,
    PRIMARY KEY ("index", salesorder_order_discount_id, salesorder_order_discount_salesorder_id),
    FOREIGN KEY (salesorder_order_discount_id) REFERENCES salesorder_order_discount(id),
    FOREIGN KEY (salesorder_order_discount_salesorder_id) REFERENCES salesorder_order_discount(salesorder_id)
);

CREATE TABLE salesorder_order_discount (
    id text,
    salesorder_id text,
    discount_type text,
    end_date text,
    end_on text,
    order_id text,
    percents text,
    start_date text,
    start_on text,
    PRIMARY KEY (id, salesorder_id),
    FOREIGN KEY (salesorder_id) REFERENCES salesorder(id)
);

CREATE TABLE salesorder (
    id text,
    account_id text,
    invoice_account_id text,
    account_receivable_account text,
    buyer_reference text,
    created text,
    currency text,
    delivery_addresscity text,
    delivery_addresscountry text,
    delivery_addresscounty text,
    delivery_addressdescription text,
    delivery_addressid text,
    delivery_addressname text,
    delivery_addressstate text,
    delivery_addressstreet text,
    delivery_addressstreet_2 text,
    delivery_addresszip text,
    description text,
    external_crm_id text,
    external_erp_id text,
    invoice_accountid text,
    invoice_addresscity text,
    invoice_addresscountry text,
    invoice_addresscounty text,
    invoice_addressdescription text,
    invoice_addressid text,
    invoice_addressname text,
    invoice_addressstate text,
    invoice_addressstreet text,
    invoice_addressstreet_2 text,
    invoice_addresszip text,
    invoice_batch_group text,
    invoice_separatly text,
    invoicing_currency text,
    is_last_version text,
    modified text,
    number text,
    order_date text,
    order_type text,
    our_reference text,
    payment_term text,
    remark text,
    use_account_invoice_batch_group text,
    version text,
    your_order_number text,
    your_reference text,
    PRIMARY KEY (id),
    FOREIGN KEY (account_id) REFERENCES account(id),
    FOREIGN KEY (invoice_account_id) REFERENCES account(id)
);

CREATE TABLE measurement (
    id text,
    account_id text,
    order_id text,
    product_id text,
    charge_charge_number text,
    charge_description text,
    charge_external_crm_id text,
    charge_external_erp_id text,
    charge_id text,
    charge_name text,
    created text,
    from_date text,
    import_date text,
    modified text,
    quantity text,
    tags text,
    PRIMARY KEY (id),
    FOREIGN KEY (account_id) REFERENCES account(id),
    FOREIGN KEY (order_id) REFERENCES salesorder(id),
    FOREIGN KEY (product_id) REFERENCES subscription_product(id)
);

CREATE TABLE quote_product_charge_price_detail (
    "index" text,
    quote_id text,
    quote_product_charge_id text,
    quote_product_id text,
    description text,
    from_quantity text,
    line_discount_amount text,
    line_discount_percent text,
    list_price text,
    price text,
    price_base text,
    tier text,
    to_quantity text,
    PRIMARY KEY ("index", quote_id, quote_product_charge_id, quote_product_id),
    FOREIGN KEY (quote_id) REFERENCES quote_product_charge(quote_id),
    FOREIGN KEY (quote_product_charge_id) REFERENCES quote_product_charge(id),
    FOREIGN KEY (quote_product_id) REFERENCES quote_product_charge(quote_product_id)
);

CREATE TABLE quote_product_charge (
    id text,
    quote_id text,
    quote_product_id text,
    acv_amount text,
    acv_base_currency_amount text,
    acv_base_currency_code text,
    acv_currency_code text,
    acv_currency_conversion_date text,
    billing_period text,
    charge_number text,
    charge_type text,
    cmrr_amount text,
    cmrr_base_currency_amount text,
    cmrr_base_currency_code text,
    cmrr_currency_code text,
    cmrr_currency_conversion_date text,
    created text,
    display_price text,
    effective_end_date text,
    effective_start_date text,
    end_on text,
    modified text,
    "name" text,
    ppu text,
    price_model text,
    price_period text,
    quantity text,
    remarks text,
    start_on text,
    tcv_amount text,
    tcv_base_currency_amount text,
    tcv_base_currency_code text,
    tcv_currency_code text,
    tcv_currency_conversion_date text,
    unit_code text,
    usage_rating text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id, quote_id, quote_product_id),
    FOREIGN KEY (quote_id) REFERENCES quote_product(quote_id),
    FOREIGN KEY (quote_product_id) REFERENCES quote_product(id)
);

CREATE TABLE quote_product (
    id text,
    quote_id text,
    charge_plan_id text,
    created text,
    modified text,
    "name" text,
    number text,
    product_line_number text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id, quote_id),
    FOREIGN KEY (quote_id) REFERENCES quote(id),
    FOREIGN KEY (charge_plan_id) REFERENCES product_charge_plan(id)
);

CREATE TABLE quote (
    id text,
    customer_id text,
    invoice_customer_id text,
    auto_renewal text,
    converted_to_orderexternal_crm_id text,
    converted_to_orderexternal_erp_id text,
    converted_to_orderid text,
    converted_to_ordernumber text,
    created text,
    currency text,
    delivery_addresscity text,
    delivery_addresscountry text,
    delivery_addresscounty text,
    delivery_addressdescription text,
    delivery_addressid text,
    delivery_addressname text,
    delivery_addressstate text,
    delivery_addressstreet text,
    delivery_addressstreet_2 text,
    delivery_addresszip text,
    description text,
    effective_end_date text,
    effective_start_date text,
    initial_term text,
    invoice_customerid text,
    modified text,
    notice_period text,
    number text,
    order_type text,
    our_reference text,
    quote_date text,
    remarks text,
    renewal_term text,
    "status" text,
    term_type text,
    valid_to_date text,
    version text,
    your_reference text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (customer_id) REFERENCES account(id),
    FOREIGN KEY (invoice_customer_id) REFERENCES account(id)
);

CREATE TABLE journal_voucher_transaction (
    "index" text,
    journal_voucher_index text,
    journal_voucher_journal_id text,
    account text,
    credit_amount text,
    custom_fields jsonb,
    debit_amount text,
    description text,
    PRIMARY KEY ("index", journal_voucher_index, journal_voucher_journal_id),
    FOREIGN KEY (journal_voucher_index) REFERENCES journal_voucher("index"),
    FOREIGN KEY (journal_voucher_journal_id) REFERENCES journal_voucher(journal_id)
);

CREATE TABLE journal_voucher (
    "index" text,
    journal_id text,
    invoice_id text,
    payment_id text,
    created text,
    effective_date text,
    external_crm_id text,
    external_erp_id text,
    modified text,
    order_charge_id text,
    order_charge_number text,
    payment_settlement_id text,
    revenue_schedule_item_id text,
    "source" text,
    transaction_number text,
    PRIMARY KEY ("index", journal_id),
    FOREIGN KEY (journal_id) REFERENCES journal(id),
    FOREIGN KEY (invoice_id) REFERENCES invoice(id),
    FOREIGN KEY (payment_id) REFERENCES payment(id)
);

CREATE TABLE journal (
    id text,
    accounting_periodcalendar_month text,
    accounting_periodcalendar_year text,
    accounting_periodclosed_date text,
    accounting_perioddescription text,
    accounting_periodend_date text,
    accounting_periodfiscal_period text,
    accounting_periodfiscal_quarter text,
    accounting_periodfiscal_year text,
    accounting_periodstart_date text,
    accounting_periodstatus text,
    created text,
    description text,
    external_erp_id text,
    modified text,
    number text,
    posted text,
    posted_date text,
    transferred_to_erp text,
    "type" text,
    PRIMARY KEY (id)
);

CREATE TABLE currency (
    id text,
    code text,
    description text,
    PRIMARY KEY (id)
);

CREATE TABLE payment_settlement_write_off (
    id text,
    payment_settlement_index text,
    payment_settlement_payment_id text,
    amount_amount text,
    amount_base_currency_amount text,
    amount_base_currency_code text,
    amount_currency_code text,
    amount_currency_conversion_date text,
    description text,
    financial_account_id text,
    financial_accountcode text,
    financial_accountdescription text,
    financial_accountexternal_crm_id text,
    financial_accountexternal_erp_id text,
    financial_accountid text,
    financial_accountname text,
    PRIMARY KEY (id, payment_settlement_index, payment_settlement_payment_id),
    FOREIGN KEY (payment_settlement_index) REFERENCES payment_settlement("index"),
    FOREIGN KEY (payment_settlement_payment_id) REFERENCES payment_settlement(payment_id)
);

CREATE TABLE payment_settlement (
    "index" text,
    payment_id text,
    invoice_id text,
    amount_amount text,
    amount_base_currency_amount text,
    amount_base_currency_code text,
    amount_currency_code text,
    amount_currency_conversion_date text,
    PRIMARY KEY ("index", payment_id),
    FOREIGN KEY (payment_id) REFERENCES payment(id),
    FOREIGN KEY (invoice_id) REFERENCES invoice(id)
);

CREATE TABLE payment_write_off (
    id text,
    payment_id text,
    amount_amount text,
    amount_base_currency_amount text,
    amount_currency_code text,
    amount_currency_conversion_date text,
    financial_accountcode text,
    financial_accountdescription text,
    financial_accountexternal_crm_id text,
    financial_accountexternal_erp_id text,
    financial_accountid text,
    financial_accountname text,
    posting_text text,
    PRIMARY KEY (id, payment_id),
    FOREIGN KEY (payment_id) REFERENCES payment(id)
);

CREATE TABLE payment (
    id text,
    amount_amount text,
    amount_base_currency_amount text,
    amount_base_currency_code text,
    amount_currency_code text,
    amount_currency_conversion_date text,
    bank_fee_accountcode text,
    bank_fee_accountdescription text,
    bank_fee_accountexternal_crm_id text,
    bank_fee_accountexternal_erp_id text,
    bank_fee_accountid text,
    bank_fee_accountname text,
    bank_fee_amountamount text,
    bank_fee_amountbase_currency_amount text,
    bank_fee_amountbase_currency_code text,
    bank_fee_amountcurrency_code text,
    bank_fee_amountcurrency_conversion_date text,
    description text,
    exchange_rate text,
    external_bank_id text,
    payer_information text,
    payer_name text,
    payment_accountcode text,
    payment_accountdescription text,
    payment_accountexternal_crm_id text,
    payment_accountexternal_erp_id text,
    payment_accountid text,
    payment_accountname text,
    payment_batch_id text,
    payment_batch_number text,
    payment_date text,
    payment_method_id text,
    payment_method_name text,
    reference text,
    PRIMARY KEY (id)
);

CREATE TABLE product_charge_plan_price_detail (
    "index" text,
    charge_plan_detail_id text,
    charge_plan_id text,
    product_id text,
    currency text,
    description text,
    from_quantity text,
    price text,
    price_base text,
    tier text,
    to_quantity text,
    PRIMARY KEY ("index", charge_plan_detail_id, charge_plan_id, product_id),
    FOREIGN KEY (charge_plan_detail_id) REFERENCES product_charge_plan_detail(id),
    FOREIGN KEY (charge_plan_id) REFERENCES product_charge_plan_detail(charge_plan_id),
    FOREIGN KEY (product_id) REFERENCES product_charge_plan_detail(product_id)
);

CREATE TABLE product_charge_plan_detail (
    id text,
    charge_plan_id text,
    product_id text,
    billing_day text,
    billing_period text,
    billing_timing text,
    charge_number text,
    charge_type text,
    create_invoice_lines_per_tier text,
    created text,
    default_quantity text,
    deferred_revenue_account text,
    external_crm_id text,
    external_erp_id text,
    model text,
    modified text,
    "name" text,
    period_alignment text,
    price_period text,
    recognized_revenue_account text,
    specific_billing_day text,
    tax_included text,
    tax_template text,
    unit_code text,
    usage_rating text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id, charge_plan_id, product_id),
    FOREIGN KEY (charge_plan_id) REFERENCES product_charge_plan(id),
    FOREIGN KEY (product_id) REFERENCES product_charge_plan(product_id)
);

CREATE TABLE product_charge_plan (
    id text,
    product_id text,
    created text,
    effective_end_date text,
    effective_start_date text,
    modified text,
    "name" text,
    number text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id, product_id),
    FOREIGN KEY (product_id) REFERENCES product(id)
);

CREATE TABLE product (
    id text,
    activation_date text,
    category text,
    created text,
    end_of_life_date text,
    end_of_new_sales_date text,
    end_of_renewal_date text,
    external_crm_id text,
    external_erp_id text,
    is_framework_product text,
    modified text,
    "name" text,
    number text,
    "type" text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id)
);

CREATE TABLE payment_term (
    id text,
    days text,
    description text,
    "name" text,
    term_type text,
    PRIMARY KEY (id)
);

CREATE TABLE exchange_rate (
    id text,
    effective_from_date text,
    effective_to_date text,
    from_currency text,
    rate text,
    to_currency text,
    PRIMARY KEY (id)
);

CREATE TABLE invoice_line (
    id text,
    invoice_id text,
    account_id text,
    order_id text,
    order_product_id text,
    charge_description text,
    charge_number text,
    charge_plan_id text,
    created text,
    deferred_revenuecode text,
    deferred_revenuedescription text,
    deferred_revenueexternal_crm_id text,
    deferred_revenueexternal_erp_id text,
    deferred_revenueid text,
    deferred_revenuename text,
    external_crm_id text,
    external_erp_id text,
    modified text,
    notes text,
    number text,
    order_charge_id text,
    price text,
    product_name text,
    product_number text,
    quantity text,
    recognized_revenuecode text,
    recognized_revenuedescription text,
    recognized_revenueexternal_crm_id text,
    recognized_revenueexternal_erp_id text,
    recognized_revenueid text,
    recognized_revenuename text,
    service_period_end_date text,
    service_period_start_date text,
    subtotal text,
    subtotal_pre_discount text,
    tax text,
    tax_category_name text,
    tax_pre_discount text,
    tax_rate text,
    total text,
    total_pre_discount text,
    unit_of_measuredisplay_name text,
    unit_of_measureid text,
    unit_of_measurename text,
    unit_of_measureunit_code text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id, invoice_id),
    FOREIGN KEY (invoice_id) REFERENCES invoice(id),
    FOREIGN KEY (account_id) REFERENCES account(id),
    FOREIGN KEY (order_id) REFERENCES salesorder(id),
    FOREIGN KEY (order_product_id) REFERENCES salesorder_product(id)
);

CREATE TABLE invoice (
    id text,
    account_id text,
    payment_term_id text,
    accounts_receivablecode text,
    accounts_receivabledescription text,
    accounts_receivableexternal_crm_id text,
    accounts_receivableexternal_erp_id text,
    accounts_receivableid text,
    accounts_receivablename text,
    balanced_amount text,
    buyer_reference text,
    created text,
    currency text,
    "date" text,
    days_past_due text,
    delivery_addresscity text,
    delivery_addresscountry text,
    delivery_addresscounty text,
    delivery_addressdescription text,
    delivery_addressid text,
    delivery_addressname text,
    delivery_addressstate text,
    delivery_addressstreet text,
    delivery_addressstreet_2 text,
    delivery_addresszip text,
    disable_automatic_invoice_reminder text,
    due_date text,
    exchange_rate text,
    external_crm_id text,
    external_erp_id text,
    invoice_addresscity text,
    invoice_addresscountry text,
    invoice_addresscounty text,
    invoice_addressdescription text,
    invoice_addressid text,
    invoice_addressname text,
    invoice_addressstate text,
    invoice_addressstreet text,
    invoice_addressstreet_2 text,
    invoice_addresszip text,
    invoice_batch_id text,
    invoice_delivery_method text,
    invoice_template_id text,
    invoice_type text,
    modified text,
    notes text,
    nr_of_reminders text,
    number text,
    ocr_number text,
    online_payment_link text,
    our_reference text,
    payment_termid text,
    send_method text,
    settled_amount text,
    settled_notes text,
    "status" text,
    subtotal text,
    tax text,
    tax_included text,
    total_amount text,
    total_rounding_amount text,
    your_order_number text,
    your_reference text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id),
    FOREIGN KEY (account_id) REFERENCES account(id),
    FOREIGN KEY (payment_term_id) REFERENCES payment_term(id)
);

CREATE TABLE tax_template_detail (
    id text,
    country_id text,
    tax_template_id text,
    country_active text,
    country_custom_fields jsonb,
    country_name text,
    country_three_alpha_code text,
    country_two_alpha_code text,
    tax_1 text,
    tax_2 text,
    tax_3 text,
    tax_categorydescription text,
    tax_categoryid text,
    tax_categoryis_active text,
    tax_categoryis_eu_tax text,
    tax_categoryname text,
    use_tax_jar text,
    PRIMARY KEY (id),
    FOREIGN KEY (country_id) REFERENCES country(id),
    FOREIGN KEY (tax_template_id) REFERENCES tax_template(id)
);

CREATE TABLE tax_template (
    id text,
    created text,
    description text,
    effective_from text,
    effective_to text,
    modified text,
    "name" text,
    tax_accountcode text,
    tax_accountdescription text,
    tax_accountexternal_crm_id text,
    tax_accountexternal_erp_id text,
    tax_accountid text,
    tax_accountname text,
    PRIMARY KEY (id)
);

CREATE TABLE account_address (
    id text,
    account_id text,
    city text,
    country text,
    county text,
    description text,
    "state" text,
    street text,
    street_2 text,
    zip text,
    PRIMARY KEY (id, account_id),
    FOREIGN KEY (account_id) REFERENCES account(id)
);

CREATE TABLE account (
    id text,
    account_type text,
    accounts_receivable text,
    acv_amount text,
    acv_base_currency_amount text,
    acv_base_currency_code text,
    acv_currency_code text,
    acv_currency_conversion_date text,
    cmrr_amount text,
    cmrr_base_currency_amount text,
    cmrr_base_currency_code text,
    cmrr_currency_code text,
    cmrr_currency_conversion_date text,
    created text,
    currency_code text,
    default_payment_term text,
    domain text,
    electronic_invoice_address text,
    electronic_invoice_address_scheme text,
    emrr_amount text,
    emrr_base_currency_amount text,
    emrr_base_currency_code text,
    emrr_currency_code text,
    emrr_currency_conversion_date text,
    external_crm_id text,
    external_erp_id text,
    image_url text,
    inactive text,
    invoice_batch_group_id text,
    invoice_delivery_method text,
    invoice_email_address text,
    invoice_email_cc_addresses text,
    invoice_setting_group_id text,
    invoice_template_id text,
    modified text,
    "name" text,
    number text,
    one_time_feesamount text,
    one_time_feesbase_currency_amount text,
    one_time_feesbase_currency_code text,
    one_time_feescurrency_code text,
    one_time_feescurrency_conversion_date text,
    online_payment_detailscreated text,
    online_payment_detailsmodified text,
    online_payment_detailsstripe_customer_id text,
    online_payment_detailsstripe_payment_method_id text,
    organization_number text,
    our_reference text,
    parent_account_id text,
    reminder_email_address text,
    tax_registration_number text,
    tax_template text,
    tcv_amount text,
    tcv_base_currency_amount text,
    tcv_base_currency_code text,
    tcv_currency_code text,
    tcv_currency_conversion_date text,
    your_reference text,
    -- custom_fields_* (dynamic column),
    PRIMARY KEY (id)
);
